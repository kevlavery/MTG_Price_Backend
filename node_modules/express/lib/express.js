/*!
 * express
 * Copyright(c) 2009-2013 TJ Holowaychuk
 * Copyright(c) 2013 Roman Shtylman
 * Copyright(c) 2014-2015 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict';

/**
 * Module dependencies.
 */

var bodyParser = require('body-parser')
var EventEmitter = require('events').EventEmitter;
var mixin = require('merge-descriptors');
var proto = require('./application');
var Route = require('./router/route');
var Router = require('./router');
var req = require('./request');
var res = require('./response');

/**
 * Expose `createApplication()`.
 */

exports = module.exports = createApplication;

/**
 * Create an express application.
 *
 * @return ***REMOVED***Function***REMOVED***
 * @api public
 */

function createApplication() ***REMOVED***
  var app = function(req, res, next) ***REMOVED***
    app.handle(req, res, next);
  ***REMOVED***;

  mixin(app, EventEmitter.prototype, false);
  mixin(app, proto, false);

  // expose the prototype that will get set on requests
  app.request = Object.create(req, ***REMOVED***
    app: ***REMOVED*** configurable: true, enumerable: true, writable: true, value: app ***REMOVED***
  ***REMOVED***)

  // expose the prototype that will get set on responses
  app.response = Object.create(res, ***REMOVED***
    app: ***REMOVED*** configurable: true, enumerable: true, writable: true, value: app ***REMOVED***
  ***REMOVED***)

  app.init();
  return app;
***REMOVED***

/**
 * Expose the prototypes.
 */

exports.application = proto;
exports.request = req;
exports.response = res;

/**
 * Expose constructors.
 */

exports.Route = Route;
exports.Router = Router;

/**
 * Expose middleware
 */

exports.json = bodyParser.json
exports.query = require('./middleware/query');
exports.static = require('serve-static');
exports.urlencoded = bodyParser.urlencoded

/**
 * Replace removed middleware with an appropriate error message.
 */

;[
  'bodyParser',
  'compress',
  'cookieSession',
  'session',
  'logger',
  'cookieParser',
  'favicon',
  'responseTime',
  'errorHandler',
  'timeout',
  'methodOverride',
  'vhost',
  'csrf',
  'directory',
  'limit',
  'multipart',
  'staticCache',
].forEach(function (name) ***REMOVED***
  Object.defineProperty(exports, name, ***REMOVED***
    get: function () ***REMOVED***
      throw new Error('Most middleware (like ' + name + ') is no longer bundled with Express and must be installed separately. Please see https://github.com/senchalabs/connect#middleware.');
    ***REMOVED***,
    configurable: true
  ***REMOVED***);
***REMOVED***);
