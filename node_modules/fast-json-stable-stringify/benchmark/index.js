'use strict';

const Benchmark = require('benchmark');
const suite = new Benchmark.Suite;
const testData = require('./test.json');


const stringifyPackages = ***REMOVED***
  // 'JSON.stringify': JSON.stringify,
  'fast-json-stable-stringify': require('../index'),
  'json-stable-stringify': true,
  'fast-stable-stringify': true,
  'faster-stable-stringify': true
***REMOVED***;


for (const name in stringifyPackages) ***REMOVED***
  let func = stringifyPackages[name];
  if (func === true) func = require(name);

  suite.add(name, function() ***REMOVED***
    func(testData);
  ***REMOVED***);
***REMOVED***

suite
  .on('cycle', (event) => console.log(String(event.target)))
  .on('complete', function () ***REMOVED***
    console.log('The fastest is ' + this.filter('fastest').map('name'));
  ***REMOVED***)
  .run(***REMOVED***async: true***REMOVED***);
