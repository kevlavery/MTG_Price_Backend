// Copyright 2015 Joyent, Inc.

module.exports = ***REMOVED***
	read: read,
	readSSHPrivate: readSSHPrivate,
	write: write
***REMOVED***;

var assert = require('assert-plus');
var asn1 = require('asn1');
var algs = require('../algs');
var utils = require('../utils');
var crypto = require('crypto');

var Key = require('../key');
var PrivateKey = require('../private-key');
var pem = require('./pem');
var rfc4253 = require('./rfc4253');
var SSHBuffer = require('../ssh-buffer');
var errors = require('../errors');

var bcrypt;

function read(buf, options) ***REMOVED***
	return (pem.read(buf, options));
***REMOVED***

var MAGIC = 'openssh-key-v1';

function readSSHPrivate(type, buf, options) ***REMOVED***
	buf = new SSHBuffer(***REMOVED***buffer: buf***REMOVED***);

	var magic = buf.readCString();
	assert.strictEqual(magic, MAGIC, 'bad magic string');

	var cipher = buf.readString();
	var kdf = buf.readString();
	var kdfOpts = buf.readBuffer();

	var nkeys = buf.readInt();
	if (nkeys !== 1) ***REMOVED***
		throw (new Error('OpenSSH-format key file contains ' +
		    'multiple keys: this is unsupported.'));
	***REMOVED***

	var pubKey = buf.readBuffer();

	if (type === 'public') ***REMOVED***
		assert.ok(buf.atEnd(), 'excess bytes left after key');
		return (rfc4253.read(pubKey));
	***REMOVED***

	var privKeyBlob = buf.readBuffer();
	assert.ok(buf.atEnd(), 'excess bytes left after key');

	var kdfOptsBuf = new SSHBuffer(***REMOVED*** buffer: kdfOpts ***REMOVED***);
	switch (kdf) ***REMOVED***
	case 'none':
		if (cipher !== 'none') ***REMOVED***
			throw (new Error('OpenSSH-format key uses KDF "none" ' +
			     'but specifies a cipher other than "none"'));
		***REMOVED***
		break;
	case 'bcrypt':
		var salt = kdfOptsBuf.readBuffer();
		var rounds = kdfOptsBuf.readInt();
		var cinf = utils.opensshCipherInfo(cipher);
		if (bcrypt === undefined) ***REMOVED***
			bcrypt = require('bcrypt-pbkdf');
		***REMOVED***

		if (typeof (options.passphrase) === 'string') ***REMOVED***
			options.passphrase = new Buffer(options.passphrase,
			    'utf-8');
		***REMOVED***
		if (!Buffer.isBuffer(options.passphrase)) ***REMOVED***
			throw (new errors.KeyEncryptedError(
			    options.filename, 'OpenSSH'));
		***REMOVED***

		var pass = new Uint8Array(options.passphrase);
		var salti = new Uint8Array(salt);
		/* Use the pbkdf to derive both the key and the IV. */
		var out = new Uint8Array(cinf.keySize + cinf.blockSize);
		var res = bcrypt.pbkdf(pass, pass.length, salti, salti.length,
		    out, out.length, rounds);
		if (res !== 0) ***REMOVED***
			throw (new Error('bcrypt_pbkdf function returned ' +
			    'failure, parameters invalid'));
		***REMOVED***
		out = new Buffer(out);
		var ckey = out.slice(0, cinf.keySize);
		var iv = out.slice(cinf.keySize, cinf.keySize + cinf.blockSize);
		var cipherStream = crypto.createDecipheriv(cinf.opensslName,
		    ckey, iv);
		cipherStream.setAutoPadding(false);
		var chunk, chunks = [];
		cipherStream.once('error', function (e) ***REMOVED***
			if (e.toString().indexOf('bad decrypt') !== -1) ***REMOVED***
				throw (new Error('Incorrect passphrase ' +
				    'supplied, could not decrypt key'));
			***REMOVED***
			throw (e);
		***REMOVED***);
		cipherStream.write(privKeyBlob);
		cipherStream.end();
		while ((chunk = cipherStream.read()) !== null)
			chunks.push(chunk);
		privKeyBlob = Buffer.concat(chunks);
		break;
	default:
		throw (new Error(
		    'OpenSSH-format key uses unknown KDF "' + kdf + '"'));
	***REMOVED***

	buf = new SSHBuffer(***REMOVED***buffer: privKeyBlob***REMOVED***);

	var checkInt1 = buf.readInt();
	var checkInt2 = buf.readInt();
	if (checkInt1 !== checkInt2) ***REMOVED***
		throw (new Error('Incorrect passphrase supplied, could not ' +
		    'decrypt key'));
	***REMOVED***

	var ret = ***REMOVED******REMOVED***;
	var key = rfc4253.readInternal(ret, 'private', buf.remainder());

	buf.skip(ret.consumed);

	var comment = buf.readString();
	key.comment = comment;

	return (key);
***REMOVED***

function write(key, options) ***REMOVED***
	var pubKey;
	if (PrivateKey.isPrivateKey(key))
		pubKey = key.toPublic();
	else
		pubKey = key;

	var cipher = 'none';
	var kdf = 'none';
	var kdfopts = new Buffer(0);
	var cinf = ***REMOVED*** blockSize: 8 ***REMOVED***;
	var passphrase;
	if (options !== undefined) ***REMOVED***
		passphrase = options.passphrase;
		if (typeof (passphrase) === 'string')
			passphrase = new Buffer(passphrase, 'utf-8');
		if (passphrase !== undefined) ***REMOVED***
			assert.buffer(passphrase, 'options.passphrase');
			assert.optionalString(options.cipher, 'options.cipher');
			cipher = options.cipher;
			if (cipher === undefined)
				cipher = 'aes128-ctr';
			cinf = utils.opensshCipherInfo(cipher);
			kdf = 'bcrypt';
		***REMOVED***
	***REMOVED***

	var privBuf;
	if (PrivateKey.isPrivateKey(key)) ***REMOVED***
		privBuf = new SSHBuffer(***REMOVED******REMOVED***);
		var checkInt = crypto.randomBytes(4).readUInt32BE(0);
		privBuf.writeInt(checkInt);
		privBuf.writeInt(checkInt);
		privBuf.write(key.toBuffer('rfc4253'));
		privBuf.writeString(key.comment || '');

		var n = 1;
		while (privBuf._offset % cinf.blockSize !== 0)
			privBuf.writeChar(n++);
		privBuf = privBuf.toBuffer();
	***REMOVED***

	switch (kdf) ***REMOVED***
	case 'none':
		break;
	case 'bcrypt':
		var salt = crypto.randomBytes(16);
		var rounds = 16;
		var kdfssh = new SSHBuffer(***REMOVED******REMOVED***);
		kdfssh.writeBuffer(salt);
		kdfssh.writeInt(rounds);
		kdfopts = kdfssh.toBuffer();

		if (bcrypt === undefined) ***REMOVED***
			bcrypt = require('bcrypt-pbkdf');
		***REMOVED***
		var pass = new Uint8Array(passphrase);
		var salti = new Uint8Array(salt);
		/* Use the pbkdf to derive both the key and the IV. */
		var out = new Uint8Array(cinf.keySize + cinf.blockSize);
		var res = bcrypt.pbkdf(pass, pass.length, salti, salti.length,
		    out, out.length, rounds);
		if (res !== 0) ***REMOVED***
			throw (new Error('bcrypt_pbkdf function returned ' +
			    'failure, parameters invalid'));
		***REMOVED***
		out = new Buffer(out);
		var ckey = out.slice(0, cinf.keySize);
		var iv = out.slice(cinf.keySize, cinf.keySize + cinf.blockSize);

		var cipherStream = crypto.createCipheriv(cinf.opensslName,
		    ckey, iv);
		cipherStream.setAutoPadding(false);
		var chunk, chunks = [];
		cipherStream.once('error', function (e) ***REMOVED***
			throw (e);
		***REMOVED***);
		cipherStream.write(privBuf);
		cipherStream.end();
		while ((chunk = cipherStream.read()) !== null)
			chunks.push(chunk);
		privBuf = Buffer.concat(chunks);
		break;
	default:
		throw (new Error('Unsupported kdf ' + kdf));
	***REMOVED***

	var buf = new SSHBuffer(***REMOVED******REMOVED***);

	buf.writeCString(MAGIC);
	buf.writeString(cipher);	/* cipher */
	buf.writeString(kdf);		/* kdf */
	buf.writeBuffer(kdfopts);	/* kdfoptions */

	buf.writeInt(1);		/* nkeys */
	buf.writeBuffer(pubKey.toBuffer('rfc4253'));

	if (privBuf)
		buf.writeBuffer(privBuf);

	buf = buf.toBuffer();

	var header;
	if (PrivateKey.isPrivateKey(key))
		header = 'OPENSSH PRIVATE KEY';
	else
		header = 'OPENSSH PUBLIC KEY';

	var tmp = buf.toString('base64');
	var len = tmp.length + (tmp.length / 70) +
	    18 + 16 + header.length*2 + 10;
	buf = new Buffer(len);
	var o = 0;
	o += buf.write('-----BEGIN ' + header + '-----\n', o);
	for (var i = 0; i < tmp.length; ) ***REMOVED***
		var limit = i + 70;
		if (limit > tmp.length)
			limit = tmp.length;
		o += buf.write(tmp.slice(i, limit), o);
		buf[o++] = 10;
		i = limit;
	***REMOVED***
	o += buf.write('-----END ' + header + '-----\n', o);

	return (buf.slice(0, o));
***REMOVED***
