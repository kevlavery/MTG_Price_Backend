#!/usr/bin/env sh

set -e

mkdir -p .browser

find spec -type f -name '*.spec.js' | \
xargs -I ***REMOVED******REMOVED*** sh -c \
'export f="***REMOVED******REMOVED***"; browserify $f -t require-globify -t brfs -x ajv -u buffer -o $(echo $f | sed -e "s/spec/.browser/");'
