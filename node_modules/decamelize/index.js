'use strict';
module.exports = function (str, sep) ***REMOVED***
	if (typeof str !== 'string') ***REMOVED***
		throw new TypeError('Expected a string');
	***REMOVED***

	sep = typeof sep === 'undefined' ? '_' : sep;

	return str
		.replace(/([a-z\d])([A-Z])/g, '$1' + sep + '$2')
		.replace(/([A-Z]+)([A-Z][a-z\d]+)/g, '$1' + sep + '$2')
		.toLowerCase();
***REMOVED***;
