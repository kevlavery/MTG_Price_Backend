// API
module.exports = abort;

/**
 * Aborts leftover active jobs
 *
 * @param ***REMOVED***object***REMOVED*** state - current state object
 */
function abort(state)
***REMOVED***
  Object.keys(state.jobs).forEach(clean.bind(state));

  // reset leftover jobs
  state.jobs = ***REMOVED******REMOVED***;
***REMOVED***

/**
 * Cleans up leftover job by invoking abort function for the provided job id
 *
 * @this  state
 * @param ***REMOVED***string|number***REMOVED*** key - job id to abort
 */
function clean(key)
***REMOVED***
  if (typeof this.jobs[key] == 'function')
  ***REMOVED***
    this.jobs[key]();
  ***REMOVED***
***REMOVED***
