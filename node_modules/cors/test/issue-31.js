(function () ***REMOVED***
  /*global describe, it*/

  'use strict';

  var should = require('should'),
    express = require('express'),
    supertest = require('supertest'),
    cors = require('../lib');

  var app,
    mainRouter,
    itemsRouter;

  /* -------------------------------------------------------------------------- */

  itemsRouter = new express.Router();
  itemsRouter.get('/', function (req, res) ***REMOVED***
    res.send('hello world');
  ***REMOVED***);

  mainRouter = new express.Router();
  mainRouter.use('/items', itemsRouter);

  app = express();
  app.use(cors());
  app.use(mainRouter);

  /* -------------------------------------------------------------------------- */

  describe('issue  #31', function () ***REMOVED***
    it('OPTIONS works', function (done) ***REMOVED***
      supertest(app)
        .options('/items')
        .expect(204)
        .set('Origin', 'http://example.com')
        .end(function (err, res) ***REMOVED***
          should.not.exist(err);
          res.headers['access-control-allow-origin'].should.eql('*');
          done();
        ***REMOVED***);
    ***REMOVED***);

    it('GET works', function (done) ***REMOVED***
      supertest(app)
        .get('/items')
        .expect(200)
        .set('Origin', 'http://example.com')
        .end(function (err, res) ***REMOVED***
          should.not.exist(err);
          res.headers['access-control-allow-origin'].should.eql('*');
          res.text.should.eql('hello world');
          done();
        ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

***REMOVED***());
