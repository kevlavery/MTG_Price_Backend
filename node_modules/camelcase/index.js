'use strict';
module.exports = function () ***REMOVED***
	var str = [].map.call(arguments, function (str) ***REMOVED***
		return str.trim();
	***REMOVED***).filter(function (str) ***REMOVED***
		return str.length;
	***REMOVED***).join('-');

	if (!str.length) ***REMOVED***
		return '';
	***REMOVED***

	if (str.length === 1 || !(/[_.\- ]+/).test(str) ) ***REMOVED***
		if (str[0] === str[0].toLowerCase() && str.slice(1) !== str.slice(1).toLowerCase()) ***REMOVED***
			return str;
		***REMOVED***

		return str.toLowerCase();
	***REMOVED***

	return str
	.replace(/^[_.\- ]+/, '')
	.toLowerCase()
	.replace(/[_.\- ]+(\w|$)/g, function (m, p1) ***REMOVED***
		return p1.toUpperCase();
	***REMOVED***);
***REMOVED***;
