// populates missing values
module.exports = function(dst, src) ***REMOVED***

  Object.keys(src).forEach(function(prop)
  ***REMOVED***
    dst[prop] = dst[prop] || src[prop];
  ***REMOVED***);

  return dst;
***REMOVED***;
