import runtime from './handlebars.runtime';

// Compiler imports
import AST from './handlebars/compiler/ast';
import ***REMOVED*** parser as Parser, parse ***REMOVED*** from './handlebars/compiler/base';
import ***REMOVED*** Compiler, compile, precompile ***REMOVED*** from './handlebars/compiler/compiler';
import JavaScriptCompiler from './handlebars/compiler/javascript-compiler';
import Visitor from './handlebars/compiler/visitor';

import noConflict from './handlebars/no-conflict';

let _create = runtime.create;
function create() ***REMOVED***
  let hb = _create();

  hb.compile = function(input, options) ***REMOVED***
    return compile(input, options, hb);
  ***REMOVED***;
  hb.precompile = function(input, options) ***REMOVED***
    return precompile(input, options, hb);
  ***REMOVED***;

  hb.AST = AST;
  hb.Compiler = Compiler;
  hb.JavaScriptCompiler = JavaScriptCompiler;
  hb.Parser = Parser;
  hb.parse = parse;

  return hb;
***REMOVED***

let inst = create();
inst.create = create;

noConflict(inst);

inst.Visitor = Visitor;

inst['default'] = inst;

export default inst;
