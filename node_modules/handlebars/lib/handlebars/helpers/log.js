export default function(instance) ***REMOVED***
  instance.registerHelper('log', function(/* message, options */) ***REMOVED***
    let args = [undefined],
        options = arguments[arguments.length - 1];
    for (let i = 0; i < arguments.length - 1; i++) ***REMOVED***
      args.push(arguments[i]);
    ***REMOVED***

    let level = 1;
    if (options.hash.level != null) ***REMOVED***
      level = options.hash.level;
    ***REMOVED*** else if (options.data && options.data.level != null) ***REMOVED***
      level = options.data.level;
    ***REMOVED***
    args[0] = level;

    instance.log(... args);
  ***REMOVED***);
***REMOVED***
