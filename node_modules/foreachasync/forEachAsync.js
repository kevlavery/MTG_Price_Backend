/*jshint -W054 */
;(function (exports) ***REMOVED***
  'use strict';

  function forEachAsync(arr, fn, thisArg) ***REMOVED***
    var dones = []
      , index = -1
      ;

    function next(BREAK, result) ***REMOVED***
      index += 1;

      if (index === arr.length || BREAK === forEachAsync.__BREAK) ***REMOVED***
        dones.forEach(function (done) ***REMOVED***
          done.call(thisArg, result);
        ***REMOVED***);
        return;
      ***REMOVED***

      fn.call(thisArg, next, arr[index], index, arr);
    ***REMOVED***

    setTimeout(next, 4);

    return ***REMOVED***
      then: function (_done) ***REMOVED***
        dones.push(_done);
        return this;
      ***REMOVED***
    ***REMOVED***;
  ***REMOVED***
  forEachAsync.__BREAK = ***REMOVED******REMOVED***;

  exports.forEachAsync = forEachAsync;
***REMOVED***('undefined' !== typeof exports && exports || new Function('return this')()));
