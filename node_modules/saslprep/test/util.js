import ***REMOVED*** setFlagsFromString ***REMOVED*** from 'v8'
import ***REMOVED*** range ***REMOVED*** from '../lib/util'
import test from 'ava'

// 984 by default.
setFlagsFromString('--stack_size=500')

test('should work', (t) => ***REMOVED***
  const list = range(1, 3)
  t.deepEqual(list, [1, 2, 3])
***REMOVED***)

test('should work for large ranges', (t) => ***REMOVED***
  t.notThrows(() => range(1, 1e6))
***REMOVED***)
