'use strict';

const Server = require('mongodb-topology-manager').Server;
const co = require('co');
const mongodb = require('mongodb');

co(function*() ***REMOVED***
  // Create new instance
  var server = new Server('mongod', ***REMOVED***
    auth: null,
    dbpath: '/data/db/27017'
  ***REMOVED***);

  // Purge the directory
  yield server.purge();

  // Start process
  yield server.start();

  const db = yield mongodb.MongoClient.connect('mongodb://localhost:27017/admin');

  yield db.addUser('passwordIsTaco', 'taco', ***REMOVED***
    roles: ['dbOwner']
  ***REMOVED***);

  console.log('done');
***REMOVED***).catch(error => ***REMOVED***
  console.error(error);
  process.exit(-1);
***REMOVED***);
