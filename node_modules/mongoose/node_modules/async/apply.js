'use strict';

Object.defineProperty(exports, "__esModule", ***REMOVED***
    value: true
***REMOVED***);

exports.default = function (fn /*, ...args*/) ***REMOVED***
    var args = (0, _slice2.default)(arguments, 1);
    return function () /*callArgs*/***REMOVED***
        var callArgs = (0, _slice2.default)(arguments);
        return fn.apply(null, args.concat(callArgs));
    ***REMOVED***;
***REMOVED***;

var _slice = require('./internal/slice');

var _slice2 = _interopRequireDefault(_slice);

function _interopRequireDefault(obj) ***REMOVED*** return obj && obj.__esModule ? obj : ***REMOVED*** default: obj ***REMOVED***; ***REMOVED***

;

/**
 * Creates a continuation function with some arguments already applied.
 *
 * Useful as a shorthand when combined with other control flow functions. Any
 * arguments passed to the returned function are added to the arguments
 * originally passed to apply.
 *
 * @name apply
 * @static
 * @memberOf module:Utils
 * @method
 * @category Util
 * @param ***REMOVED***Function***REMOVED*** fn - The function you want to eventually apply all
 * arguments to. Invokes with (arguments...).
 * @param ***REMOVED***...****REMOVED*** arguments... - Any number of arguments to automatically apply
 * when the continuation is called.
 * @returns ***REMOVED***Function***REMOVED*** the partially-applied function
 * @example
 *
 * // using apply
 * async.parallel([
 *     async.apply(fs.writeFile, 'testfile1', 'test1'),
 *     async.apply(fs.writeFile, 'testfile2', 'test2')
 * ]);
 *
 *
 * // the same process without using apply
 * async.parallel([
 *     function(callback) ***REMOVED***
 *         fs.writeFile('testfile1', 'test1', callback);
 *     ***REMOVED***,
 *     function(callback) ***REMOVED***
 *         fs.writeFile('testfile2', 'test2', callback);
 *     ***REMOVED***
 * ]);
 *
 * // It's possible to pass any number of additional arguments when calling the
 * // continuation:
 *
 * node> var fn = async.apply(sys.puts, 'one');
 * node> fn('two', 'three');
 * one
 * two
 * three
 */
module.exports = exports['default'];