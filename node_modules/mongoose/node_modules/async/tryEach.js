'use strict';

Object.defineProperty(exports, "__esModule", ***REMOVED***
    value: true
***REMOVED***);
exports.default = tryEach;

var _noop = require('lodash/noop');

var _noop2 = _interopRequireDefault(_noop);

var _eachSeries = require('./eachSeries');

var _eachSeries2 = _interopRequireDefault(_eachSeries);

var _wrapAsync = require('./internal/wrapAsync');

var _wrapAsync2 = _interopRequireDefault(_wrapAsync);

var _slice = require('./internal/slice');

var _slice2 = _interopRequireDefault(_slice);

function _interopRequireDefault(obj) ***REMOVED*** return obj && obj.__esModule ? obj : ***REMOVED*** default: obj ***REMOVED***; ***REMOVED***

/**
 * It runs each task in series but stops whenever any of the functions were
 * successful. If one of the tasks were successful, the `callback` will be
 * passed the result of the successful task. If all tasks fail, the callback
 * will be passed the error and result (if any) of the final attempt.
 *
 * @name tryEach
 * @static
 * @memberOf module:ControlFlow
 * @method
 * @category Control Flow
 * @param ***REMOVED***Array|Iterable|Object***REMOVED*** tasks - A collection containing functions to
 * run, each function is passed a `callback(err, result)` it must call on
 * completion with an error `err` (which can be `null`) and an optional `result`
 * value.
 * @param ***REMOVED***Function***REMOVED*** [callback] - An optional callback which is called when one
 * of the tasks has succeeded, or all have failed. It receives the `err` and
 * `result` arguments of the last attempt at completing the `task`. Invoked with
 * (err, results).
 * @example
 * async.tryEach([
 *     function getDataFromFirstWebsite(callback) ***REMOVED***
 *         // Try getting the data from the first website
 *         callback(err, data);
 *     ***REMOVED***,
 *     function getDataFromSecondWebsite(callback) ***REMOVED***
 *         // First website failed,
 *         // Try getting the data from the backup website
 *         callback(err, data);
 *     ***REMOVED***
 * ],
 * // optional callback
 * function(err, results) ***REMOVED***
 *     Now do something with the data.
 * ***REMOVED***);
 *
 */
function tryEach(tasks, callback) ***REMOVED***
    var error = null;
    var result;
    callback = callback || _noop2.default;
    (0, _eachSeries2.default)(tasks, function (task, callback) ***REMOVED***
        (0, _wrapAsync2.default)(task)(function (err, res /*, ...args*/) ***REMOVED***
            if (arguments.length > 2) ***REMOVED***
                result = (0, _slice2.default)(arguments, 1);
            ***REMOVED*** else ***REMOVED***
                result = res;
            ***REMOVED***
            error = err;
            callback(!err);
        ***REMOVED***);
    ***REMOVED***, function () ***REMOVED***
        callback(error, result);
    ***REMOVED***);
***REMOVED***
module.exports = exports['default'];