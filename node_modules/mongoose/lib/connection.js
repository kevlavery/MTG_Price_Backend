'use strict';

/*!
 * Module dependencies.
 */

const EventEmitter = require('events').EventEmitter;
const driver = global.MONGOOSE_DRIVER_PATH || './drivers/node-mongodb-native';
const Schema = require('./schema');
const Collection = require(driver + '/collection');
const STATES = require('./connectionstate');
const MongooseError = require('./error');
const PromiseProvider = require('./promise_provider');
const get = require('lodash.get');
const mongodb = require('mongodb');
const utils = require('./utils');

/*!
 * A list of authentication mechanisms that don't require a password for authentication.
 * This is used by the authMechanismDoesNotRequirePassword method.
 *
 * @api private
 */
var authMechanismsWhichDontRequirePassword = [
  'MONGODB-X509'
];

/**
 * Connection constructor
 *
 * For practical reasons, a Connection equals a Db.
 *
 * @param ***REMOVED***Mongoose***REMOVED*** base a mongoose instance
 * @inherits NodeJS EventEmitter http://nodejs.org/api/events.html#events_class_events_eventemitter
 * @event `connecting`: Emitted when `connection.***REMOVED***open,openSet***REMOVED***()` is executed on this connection.
 * @event `connected`: Emitted when this connection successfully connects to the db. May be emitted _multiple_ times in `reconnected` scenarios.
 * @event `open`: Emitted after we `connected` and `onOpen` is executed on all of this connections models.
 * @event `disconnecting`: Emitted when `connection.close()` was executed.
 * @event `disconnected`: Emitted after getting disconnected from the db.
 * @event `close`: Emitted after we `disconnected` and `onClose` executed on all of this connections models.
 * @event `reconnected`: Emitted after we `connected` and subsequently `disconnected`, followed by successfully another successfull connection.
 * @event `error`: Emitted when an error occurs on this connection.
 * @event `fullsetup`: Emitted in a replica-set scenario, when primary and at least one seconaries specified in the connection string are connected.
 * @event `all`: Emitted in a replica-set scenario, when all nodes specified in the connection string are connected.
 * @api public
 */

function Connection(base) ***REMOVED***
  this.base = base;
  this.collections = ***REMOVED******REMOVED***;
  this.models = ***REMOVED******REMOVED***;
  this.config = ***REMOVED***autoIndex: true***REMOVED***;
  this.replica = false;
  this.options = null;
  this.otherDbs = []; // FIXME: To be replaced with relatedDbs
  this.relatedDbs = ***REMOVED******REMOVED***; // Hashmap of other dbs that share underlying connection
  this.states = STATES;
  this._readyState = STATES.disconnected;
  this._closeCalled = false;
  this._hasOpened = false;
***REMOVED***

/*!
 * Inherit from EventEmitter
 */

Connection.prototype.__proto__ = EventEmitter.prototype;

/**
 * Connection ready state
 *
 * - 0 = disconnected
 * - 1 = connected
 * - 2 = connecting
 * - 3 = disconnecting
 *
 * Each state change emits its associated event name.
 *
 * ####Example
 *
 *     conn.on('connected', callback);
 *     conn.on('disconnected', callback);
 *
 * @property readyState
 * @memberOf Connection
 * @api public
 */

Object.defineProperty(Connection.prototype, 'readyState', ***REMOVED***
  get: function() ***REMOVED***
    return this._readyState;
  ***REMOVED***,
  set: function(val) ***REMOVED***
    if (!(val in STATES)) ***REMOVED***
      throw new Error('Invalid connection state: ' + val);
    ***REMOVED***

    if (this._readyState !== val) ***REMOVED***
      this._readyState = val;
      // [legacy] loop over the otherDbs on this connection and change their state
      for (var i = 0; i < this.otherDbs.length; i++) ***REMOVED***
        this.otherDbs[i].readyState = val;
      ***REMOVED***

      // loop over relatedDbs on this connection and change their state
      for (var k in this.relatedDbs) ***REMOVED***
        this.relatedDbs[k].readyState = val;
      ***REMOVED***

      if (STATES.connected === val) ***REMOVED***
        this._hasOpened = true;
      ***REMOVED***

      this.emit(STATES[val]);
    ***REMOVED***
  ***REMOVED***
***REMOVED***);

/**
 * A hash of the collections associated with this connection
 *
 * @property collections
 * @memberOf Connection
 * @api public
 */

Connection.prototype.collections;

/**
 * The name of the database this connection points to.
 *
 * ####Example
 *
 *     mongoose.createConnection('mongodb://localhost:27017/mydb').name; // "mydb"
 *
 * @property name
 * @memberOf Connection
 * @api public
 */

Connection.prototype.name;

/**
 * The host name portion of the URI. If multiple hosts, such as a replica set,
 * this will contain the first host name in the URI
 *
 * ####Example
 *
 *     mongoose.createConnection('mongodb://localhost:27017/mydb').host; // "localhost"
 *
 * @property host
 * @memberOf Connection
 * @api public
 */

Object.defineProperty(Connection.prototype, 'host', ***REMOVED***
  get: function() ***REMOVED***
    return get(this, 'client.s.options.servers.0.host', null) ||
      get(this, 'client.s.options.servers.0.domain_socket', null);
  ***REMOVED***
***REMOVED***);

/**
 * The port portion of the URI. If multiple hosts, such as a replica set,
 * this will contain the port from the first host name in the URI.
 *
 * ####Example
 *
 *     mongoose.createConnection('mongodb://localhost:27017/mydb').port; // 27017
 *
 * @property port
 * @memberOf Connection
 * @api public
 */

Object.defineProperty(Connection.prototype, 'port', ***REMOVED***
  get: function() ***REMOVED***
    return get(this, 'client.s.options.servers.0.port', 27017);
  ***REMOVED***
***REMOVED***);

/**
 * The username specified in the URI
 *
 * ####Example
 *
 *     mongoose.createConnection('mongodb://val:psw@localhost:27017/mydb').user; // "val"
 *
 * @property user
 * @memberOf Connection
 * @api public
 */

Object.defineProperty(Connection.prototype, 'user', ***REMOVED***
  get: function() ***REMOVED***
    return get(this, 'client.s.options.auth.user', null);
  ***REMOVED***
***REMOVED***);

/**
 * The password specified in the URI
 *
 * ####Example
 *
 *     mongoose.createConnection('mongodb://val:psw@localhost:27017/mydb').pass; // "psw"
 *
 * @property pass
 * @memberOf Connection
 * @api public
 */

Object.defineProperty(Connection.prototype, 'pass', ***REMOVED***
  get: function() ***REMOVED***
    return get(this, 'client.s.options.auth.password', null);
  ***REMOVED***
***REMOVED***);

/**
 * The mongodb.Db instance, set when the connection is opened
 *
 * @property db
 * @memberOf Connection
 * @api public
 */

Connection.prototype.db;

/**
 * A hash of the global options that are associated with this connection
 *
 * @property config
 * @memberOf Connection
 * @api public
 */

Connection.prototype.config;

/**
 * Helper for `createCollection()`. Will explicitly create the given collection
 * with specified options. Used to create [capped collections](https://docs.mongodb.com/manual/core/capped-collections/)
 * and [views](https://docs.mongodb.com/manual/core/views/) from mongoose.
 *
 * Options are passed down without modification to the [MongoDB driver's `createCollection()` function](http://mongodb.github.io/node-mongodb-native/2.2/api/Db.html#createCollection)
 *
 * @method createCollection
 * @param ***REMOVED***string***REMOVED*** collection The collection to create
 * @param ***REMOVED***Object***REMOVED*** [options] see [MongoDB driver docs](http://mongodb.github.io/node-mongodb-native/2.2/api/Db.html#createCollection)
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 */

Connection.prototype.createCollection = _wrapConnHelper(function createCollection(collection, options, cb) ***REMOVED***
  if (typeof options === 'function') ***REMOVED***
    cb = options;
    options = ***REMOVED******REMOVED***;
  ***REMOVED***
  this.db.createCollection(collection, options, cb);
***REMOVED***);

/**
 * Helper for `dropCollection()`. Will delete the given collection, including
 * all documents and indexes.
 *
 * @method dropCollection
 * @param ***REMOVED***string***REMOVED*** collection The collection to delete
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 */

Connection.prototype.dropCollection = _wrapConnHelper(function dropCollection(collection, cb) ***REMOVED***
  this.db.dropCollection(collection, cb);
***REMOVED***);

/**
 * Helper for `dropDatabase()`. Deletes the given database, including all
 * collections, documents, and indexes.
 *
 * @method dropDatabase
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 */

Connection.prototype.dropDatabase = _wrapConnHelper(function dropDatabase(cb) ***REMOVED***
  this.db.dropDatabase(cb);
***REMOVED***);

/*!
 * ignore
 */

function _wrapConnHelper(fn) ***REMOVED***
  return function() ***REMOVED***
    var cb = arguments.length > 0 ? arguments[arguments.length - 1] : null;
    var argsWithoutCb = typeof cb === 'function' ?
      Array.prototype.slice.call(arguments, 0, arguments.length - 1) :
      Array.prototype.slice.call(arguments);
    return utils.promiseOrCallback(cb, cb => ***REMOVED***
      if (this.readyState !== STATES.connected) ***REMOVED***
        this.on('open', function() ***REMOVED***
          fn.apply(this, argsWithoutCb.concat([function(error) ***REMOVED***
            cb(error);
          ***REMOVED***]));
        ***REMOVED***);
      ***REMOVED*** else ***REMOVED***
        fn.apply(this, argsWithoutCb.concat([function(error) ***REMOVED***
          cb(error);
        ***REMOVED***]));
      ***REMOVED***
    ***REMOVED***);
  ***REMOVED***;
***REMOVED***

/**
 * error
 *
 * Graceful error handling, passes error to callback
 * if available, else emits error on the connection.
 *
 * @param ***REMOVED***Error***REMOVED*** err
 * @param ***REMOVED***Function***REMOVED*** callback optional
 * @api private
 */

Connection.prototype.error = function(err, callback) ***REMOVED***
  if (callback) ***REMOVED***
    callback(err);
    return null;
  ***REMOVED***
  if (this.listeners('error').length > 0) ***REMOVED***
    this.emit('error', err);
  ***REMOVED***
  return Promise.reject(err);
***REMOVED***;

/**
 * Called when the connection is opened
 *
 * @api private
 */

Connection.prototype.onOpen = function() ***REMOVED***
  this.readyState = STATES.connected;

  // avoid having the collection subscribe to our event emitter
  // to prevent 0.3 warning
  for (var i in this.collections) ***REMOVED***
    if (utils.object.hasOwnProperty(this.collections, i)) ***REMOVED***
      this.collections[i].onOpen();
    ***REMOVED***
  ***REMOVED***

  this.emit('open');
***REMOVED***;

/**
 * Opens the connection with a URI using `MongoClient.connect()`.
 *
 * @param ***REMOVED***String***REMOVED*** uri The URI to connect with.
 * @param ***REMOVED***Object***REMOVED*** [options] Passed on to http://mongodb.github.io/node-mongodb-native/2.2/api/MongoClient.html#connect
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @returns ***REMOVED***Connection***REMOVED*** this
 * @api private
 */

Connection.prototype.openUri = function(uri, options, callback) ***REMOVED***
  this.readyState = STATES.connecting;
  this._closeCalled = false;

  if (typeof options === 'function') ***REMOVED***
    callback = options;
    options = null;
  ***REMOVED***

  const Promise = PromiseProvider.get();
  const _this = this;

  if (options) ***REMOVED***
    options = utils.clone(options);
    const autoIndex = options.config && options.config.autoIndex != null ?
      options.config.autoIndex :
      options.autoIndex;
    if (autoIndex != null) ***REMOVED***
      this.config.autoIndex = autoIndex !== false;
      delete options.config;
      delete options.autoIndex;
    ***REMOVED***

    // Backwards compat
    if (options.user || options.pass) ***REMOVED***
      options.auth = options.auth || ***REMOVED******REMOVED***;
      options.auth.user = options.user;
      options.auth.password = options.pass;
    ***REMOVED***
    delete options.user;
    delete options.pass;

    if (options.bufferCommands != null) ***REMOVED***
      options.bufferMaxEntries = 0;
      this.config.bufferCommands = options.bufferCommands;
      delete options.bufferCommands;
    ***REMOVED***

    if (options.useMongoClient != null) ***REMOVED***
      handleUseMongoClient(options);
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    options = ***REMOVED******REMOVED***;
  ***REMOVED***

  this._connectionOptions = options;
  let dbName = options.dbName;
  if (dbName != null) ***REMOVED***
    this.$dbName = dbName;
  ***REMOVED***
  delete options.dbName;

  if (!('promiseLibrary' in options)) ***REMOVED***
    options.promiseLibrary = PromiseProvider.get();
  ***REMOVED***

  const promise = new Promise((resolve, reject) => ***REMOVED***
    let client = new mongodb.MongoClient(uri, options);
    _this.client = client;
    client.connect(function(error) ***REMOVED***
      if (error) ***REMOVED***
        _this.readyState = STATES.disconnected;
        if (_this.listeners('error').length > 0) ***REMOVED***
          _this.emit('error', error);
        ***REMOVED***
        return reject(error);
      ***REMOVED***

      const db = dbName != null ? client.db(dbName) : client.db();
      _this.db = db;

      // Backwards compat for mongoose 4.x
      db.on('reconnect', function() ***REMOVED***
        _this.readyState = STATES.connected;
        _this.emit('reconnect');
        _this.emit('reconnected');
      ***REMOVED***);
      db.s.topology.on('reconnectFailed', function() ***REMOVED***
        _this.emit('reconnectFailed');
      ***REMOVED***);
      db.s.topology.on('left', function(data) ***REMOVED***
        _this.emit('left', data);
      ***REMOVED***);
      db.s.topology.on('joined', function(data) ***REMOVED***
        _this.emit('joined', data);
      ***REMOVED***);
      db.s.topology.on('fullsetup', function(data) ***REMOVED***
        _this.emit('fullsetup', data);
      ***REMOVED***);
      db.on('close', function() ***REMOVED***
        // Implicitly emits 'disconnected'
        _this.readyState = STATES.disconnected;
      ***REMOVED***);
      db.on('timeout', function() ***REMOVED***
        _this.emit('timeout');
      ***REMOVED***);

      delete _this.then;
      delete _this.catch;
      _this.readyState = STATES.connected;

      for (let i in _this.collections) ***REMOVED***
        if (utils.object.hasOwnProperty(_this.collections, i)) ***REMOVED***
          _this.collections[i].onOpen();
        ***REMOVED***
      ***REMOVED***

      resolve(_this);
      _this.emit('open');
    ***REMOVED***);

    _this.name = dbName != null ?
      dbName :
      get(_this, 'client.s.options.dbName', null);
  ***REMOVED***);

  if (callback != null) ***REMOVED***
    promise.then(() => callback(null, this), err => callback(err));
  ***REMOVED***

  this.$initialConnection = promise;
  this.then = function(resolve, reject) ***REMOVED***
    return promise.then(resolve, reject);
  ***REMOVED***;
  this.catch = function(reject) ***REMOVED***
    return promise.catch(reject);
  ***REMOVED***;

  return this;
***REMOVED***;

/*!
 * ignore
 */

const handleUseMongoClient = function handleUseMongoClient(options) ***REMOVED***
  console.warn('WARNING: The `useMongoClient` option is no longer ' +
    'necessary in mongoose 5.x, please remove it.');
  const stack = new Error().stack;
  console.warn(stack.substr(stack.indexOf('\n') + 1));
  delete options.useMongoClient;
***REMOVED***;

/**
 * Closes the connection
 *
 * @param ***REMOVED***Boolean***REMOVED*** [force] optional
 * @param ***REMOVED***Function***REMOVED*** [callback] optional
 * @return ***REMOVED***Connection***REMOVED*** self
 * @api public
 */

Connection.prototype.close = function(force, callback) ***REMOVED***
  if (typeof force === 'function') ***REMOVED***
    callback = force;
    force = false;
  ***REMOVED***

  this.$wasForceClosed = !!force;

  return utils.promiseOrCallback(callback, cb => ***REMOVED***
    this._close(force, cb);
  ***REMOVED***);
***REMOVED***;

/**
 * Handles closing the connection
 *
 * @param ***REMOVED***Boolean***REMOVED*** force
 * @param ***REMOVED***Function***REMOVED*** callback
 * @api private
 */
Connection.prototype._close = function(force, callback) ***REMOVED***
  var _this = this;
  this._closeCalled = true;

  switch (this.readyState) ***REMOVED***
    case 0: // disconnected
      callback();
      break;

    case 1: // connected
      this.readyState = STATES.disconnecting;
      this.doClose(force, function(err) ***REMOVED***
        if (err) ***REMOVED***
          return callback(err);
        ***REMOVED***
        _this.onClose(force);
        callback(null);
      ***REMOVED***);

      break;
    case 2: // connecting
      this.once('open', function() ***REMOVED***
        _this.close(callback);
      ***REMOVED***);
      break;

    case 3: // disconnecting
      this.once('close', function() ***REMOVED***
        callback();
      ***REMOVED***);
      break;
  ***REMOVED***

  return this;
***REMOVED***;

/**
 * Called when the connection closes
 *
 * @api private
 */

Connection.prototype.onClose = function(force) ***REMOVED***
  this.readyState = STATES.disconnected;

  // avoid having the collection subscribe to our event emitter
  // to prevent 0.3 warning
  for (var i in this.collections) ***REMOVED***
    if (utils.object.hasOwnProperty(this.collections, i)) ***REMOVED***
      this.collections[i].onClose(force);
    ***REMOVED***
  ***REMOVED***

  this.emit('close', force);
***REMOVED***;

/**
 * Retrieves a collection, creating it if not cached.
 *
 * Not typically needed by applications. Just talk to your collection through your model.
 *
 * @param ***REMOVED***String***REMOVED*** name of the collection
 * @param ***REMOVED***Object***REMOVED*** [options] optional collection options
 * @return ***REMOVED***Collection***REMOVED*** collection instance
 * @api public
 */

Connection.prototype.collection = function(name, options) ***REMOVED***
  options = options ? utils.clone(options) : ***REMOVED******REMOVED***;
  options.$wasForceClosed = this.$wasForceClosed;
  if (!(name in this.collections)) ***REMOVED***
    this.collections[name] = new Collection(name, this, options);
  ***REMOVED***
  return this.collections[name];
***REMOVED***;

/**
 * Defines or retrieves a model.
 *
 *     var mongoose = require('mongoose');
 *     var db = mongoose.createConnection(..);
 *     db.model('Venue', new Schema(..));
 *     var Ticket = db.model('Ticket', new Schema(..));
 *     var Venue = db.model('Venue');
 *
 * _When no `collection` argument is passed, Mongoose produces a collection name by passing the model `name` to the [utils.toCollectionName](#utils_exports.toCollectionName) method. This method pluralizes the name. If you don't like this behavior, either pass a collection name or set your schemas collection name option._
 *
 * ####Example:
 *
 *     var schema = new Schema(***REMOVED*** name: String ***REMOVED***, ***REMOVED*** collection: 'actor' ***REMOVED***);
 *
 *     // or
 *
 *     schema.set('collection', 'actor');
 *
 *     // or
 *
 *     var collectionName = 'actor'
 *     var M = conn.model('Actor', schema, collectionName)
 *
 * @param ***REMOVED***String***REMOVED*** name the model name
 * @param ***REMOVED***Schema***REMOVED*** [schema] a schema. necessary when defining a model
 * @param ***REMOVED***String***REMOVED*** [collection] name of mongodb collection (optional) if not given it will be induced from model name
 * @see Mongoose#model #index_Mongoose-model
 * @return ***REMOVED***Model***REMOVED*** The compiled model
 * @api public
 */

Connection.prototype.model = function(name, schema, collection) ***REMOVED***
  // collection name discovery
  if (typeof schema === 'string') ***REMOVED***
    collection = schema;
    schema = false;
  ***REMOVED***

  if (utils.isObject(schema) && !schema.instanceOfSchema) ***REMOVED***
    schema = new Schema(schema);
  ***REMOVED***
  if (schema && !schema.instanceOfSchema) ***REMOVED***
    throw new Error('The 2nd parameter to `mongoose.model()` should be a ' +
      'schema or a POJO');
  ***REMOVED***

  if (this.models[name] && !collection) ***REMOVED***
    // model exists but we are not subclassing with custom collection
    if (schema && schema.instanceOfSchema && schema !== this.models[name].schema) ***REMOVED***
      throw new MongooseError.OverwriteModelError(name);
    ***REMOVED***
    return this.models[name];
  ***REMOVED***

  var opts = ***REMOVED***cache: false, connection: this***REMOVED***;
  var model;

  if (schema && schema.instanceOfSchema) ***REMOVED***
    // compile a model
    model = this.base.model(name, schema, collection, opts);

    // only the first model with this name is cached to allow
    // for one-offs with custom collection names etc.
    if (!this.models[name]) ***REMOVED***
      this.models[name] = model;
    ***REMOVED***

    // Errors handled internally, so safe to ignore error
    model.init(() => ***REMOVED******REMOVED***);
    return model;
  ***REMOVED***

  if (this.models[name] && collection) ***REMOVED***
    // subclassing current model with alternate collection
    model = this.models[name];
    schema = model.prototype.schema;
    var sub = model.__subclass(this, schema, collection);
    // do not cache the sub model
    return sub;
  ***REMOVED***

  // lookup model in mongoose module
  model = this.base.models[name];

  if (!model) ***REMOVED***
    throw new MongooseError.MissingSchemaError(name);
  ***REMOVED***

  if (this === model.prototype.db
      && (!collection || collection === model.collection.name)) ***REMOVED***
    // model already uses this connection.

    // only the first model with this name is cached to allow
    // for one-offs with custom collection names etc.
    if (!this.models[name]) ***REMOVED***
      this.models[name] = model;
    ***REMOVED***

    return model;
  ***REMOVED***
  this.models[name] = model.__subclass(this, schema, collection);
  return this.models[name];
***REMOVED***;

/**
 * Returns an array of model names created on this connection.
 * @api public
 * @return ***REMOVED***Array***REMOVED***
 */

Connection.prototype.modelNames = function() ***REMOVED***
  return Object.keys(this.models);
***REMOVED***;

/**
 * @brief Returns if the connection requires authentication after it is opened. Generally if a
 * username and password are both provided than authentication is needed, but in some cases a
 * password is not required.
 * @api private
 * @return ***REMOVED***Boolean***REMOVED*** true if the connection should be authenticated after it is opened, otherwise false.
 */
Connection.prototype.shouldAuthenticate = function() ***REMOVED***
  return (this.user !== null && this.user !== void 0) &&
      ((this.pass !== null || this.pass !== void 0) || this.authMechanismDoesNotRequirePassword());
***REMOVED***;

/**
 * @brief Returns a boolean value that specifies if the current authentication mechanism needs a
 * password to authenticate according to the auth objects passed into the open/openSet methods.
 * @api private
 * @return ***REMOVED***Boolean***REMOVED*** true if the authentication mechanism specified in the options object requires
 *  a password, otherwise false.
 */
Connection.prototype.authMechanismDoesNotRequirePassword = function() ***REMOVED***
  if (this.options && this.options.auth) ***REMOVED***
    return authMechanismsWhichDontRequirePassword.indexOf(this.options.auth.authMechanism) >= 0;
  ***REMOVED***
  return true;
***REMOVED***;

/**
 * @brief Returns a boolean value that specifies if the provided objects object provides enough
 * data to authenticate with. Generally this is true if the username and password are both specified
 * but in some authentication methods, a password is not required for authentication so only a username
 * is required.
 * @param ***REMOVED***Object***REMOVED*** [options] the options object passed into the open/openSet methods.
 * @api private
 * @return ***REMOVED***Boolean***REMOVED*** true if the provided options object provides enough data to authenticate with,
 *   otherwise false.
 */
Connection.prototype.optionsProvideAuthenticationData = function(options) ***REMOVED***
  return (options) &&
      (options.user) &&
      ((options.pass) || this.authMechanismDoesNotRequirePassword());
***REMOVED***;

/*!
 * Module exports.
 */

Connection.STATES = STATES;
module.exports = Connection;
