'use strict';

const isDefiningProjection = require('./isDefiningProjection');

/*!
 * ignore
 */

module.exports = function isExclusive(projection) ***REMOVED***
  let keys = Object.keys(projection);
  let ki = keys.length;
  let exclude = null;

  if (ki === 1 && keys[0] === '_id') ***REMOVED***
    exclude = !!projection[keys[ki]];
  ***REMOVED*** else ***REMOVED***
    while (ki--) ***REMOVED***
      // Does this projection explicitly define inclusion/exclusion?
      // Explicitly avoid `$meta` and `$slice`
      if (keys[ki] !== '_id' && isDefiningProjection(projection[keys[ki]])) ***REMOVED***
        exclude = !projection[keys[ki]];
        break;
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  return exclude;
***REMOVED***;
