'use strict';

var isDefiningProjection = require('./isDefiningProjection');

/*!
 * ignore
 */

module.exports = function isInclusive(projection) ***REMOVED***
  if (projection == null) ***REMOVED***
    return false;
  ***REMOVED***

  var props = Object.keys(projection);
  var numProps = props.length;
  if (numProps === 0) ***REMOVED***
    return false;
  ***REMOVED***

  for (var i = 0; i < numProps; ++i) ***REMOVED***
    var prop = props[i];
    // If field is truthy (1, true, etc.) and not an object, then this
    // projection must be inclusive. If object, assume its $meta, $slice, etc.
    if (isDefiningProjection(projection[prop]) && !!projection[prop]) ***REMOVED***
      return true;
    ***REMOVED***
  ***REMOVED***

  return false;
***REMOVED***;
