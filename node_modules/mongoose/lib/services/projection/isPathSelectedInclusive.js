'use strict';

/*!
 * ignore
 */

module.exports = function isPathSelectedInclusive(fields, path) ***REMOVED***
  var chunks = path.split('.');
  var cur = '';
  var j;
  var keys;
  var numKeys;
  for (var i = 0; i < chunks.length; ++i) ***REMOVED***
    cur += cur.length ? '.' : '' + chunks[i];
    if (fields[cur]) ***REMOVED***
      keys = Object.keys(fields);
      numKeys = keys.length;
      for (j = 0; j < numKeys; ++j) ***REMOVED***
        if (keys[i].indexOf(cur + '.') === 0 && keys[i].indexOf(path) !== 0) ***REMOVED***
          continue;
        ***REMOVED***
      ***REMOVED***
      return true;
    ***REMOVED***
  ***REMOVED***

  return false;
***REMOVED***;
