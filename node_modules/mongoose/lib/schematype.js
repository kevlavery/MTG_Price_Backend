'use strict';

/*!
 * Module dependencies.
 */

var $exists = require('./schema/operators/exists');
var $type = require('./schema/operators/type');
var utils = require('./utils');
var MongooseError = require('./error');
var CastError = MongooseError.CastError;
var ValidatorError = MongooseError.ValidatorError;

/**
 * SchemaType constructor. Do **not** instantiate `SchemaType` directly.
 * Mongoose converts your schema paths into SchemaTypes automatically.
 *
 * ####Example:
 *
 *     const schema = new Schema(***REMOVED*** name: String ***REMOVED***);
 *     schema.path('name') instanceof SchemaType; // true
 *
 * @param ***REMOVED***String***REMOVED*** path
 * @param ***REMOVED***Object***REMOVED*** [options]
 * @param ***REMOVED***String***REMOVED*** [instance]
 * @api public
 */

function SchemaType(path, options, instance) ***REMOVED***
  this.path = path;
  this.instance = instance;
  this.validators = [];
  this.setters = [];
  this.getters = [];
  this.options = options;
  this._index = null;
  this.selected;

  for (var prop in options) ***REMOVED***
    if (this[prop] && typeof this[prop] === 'function') ***REMOVED***
      // ***REMOVED*** unique: true, index: true ***REMOVED***
      if (prop === 'index' && this._index) ***REMOVED***
        continue;
      ***REMOVED***

      var val = options[prop];
      // Special case so we don't screw up array defaults, see gh-5780
      if (prop === 'default') ***REMOVED***
        this.default(val);
        continue;
      ***REMOVED***

      var opts = Array.isArray(val) ? val : [val];

      this[prop].apply(this, opts);
    ***REMOVED***
  ***REMOVED***

  Object.defineProperty(this, '$$context', ***REMOVED***
    enumerable: false,
    configurable: false,
    writable: true,
    value: null
  ***REMOVED***);
***REMOVED***

/**
 * Sets a default value for this SchemaType.
 *
 * ####Example:
 *
 *     var schema = new Schema(***REMOVED*** n: ***REMOVED*** type: Number, default: 10 ***REMOVED***)
 *     var M = db.model('M', schema)
 *     var m = new M;
 *     console.log(m.n) // 10
 *
 * Defaults can be either `functions` which return the value to use as the default or the literal value itself. Either way, the value will be cast based on its schema type before being set during document creation.
 *
 * ####Example:
 *
 *     // values are cast:
 *     var schema = new Schema(***REMOVED*** aNumber: ***REMOVED*** type: Number, default: 4.815162342 ***REMOVED******REMOVED***)
 *     var M = db.model('M', schema)
 *     var m = new M;
 *     console.log(m.aNumber) // 4.815162342
 *
 *     // default unique objects for Mixed types:
 *     var schema = new Schema(***REMOVED*** mixed: Schema.Types.Mixed ***REMOVED***);
 *     schema.path('mixed').default(function () ***REMOVED***
 *       return ***REMOVED******REMOVED***;
 *     ***REMOVED***);
 *
 *     // if we don't use a function to return object literals for Mixed defaults,
 *     // each document will receive a reference to the same object literal creating
 *     // a "shared" object instance:
 *     var schema = new Schema(***REMOVED*** mixed: Schema.Types.Mixed ***REMOVED***);
 *     schema.path('mixed').default(***REMOVED******REMOVED***);
 *     var M = db.model('M', schema);
 *     var m1 = new M;
 *     m1.mixed.added = 1;
 *     console.log(m1.mixed); // ***REMOVED*** added: 1 ***REMOVED***
 *     var m2 = new M;
 *     console.log(m2.mixed); // ***REMOVED*** added: 1 ***REMOVED***
 *
 * @param ***REMOVED***Function|any***REMOVED*** val the default value
 * @return ***REMOVED***defaultValue***REMOVED***
 * @api public
 */

SchemaType.prototype.default = function(val) ***REMOVED***
  if (arguments.length === 1) ***REMOVED***
    if (val === void 0) ***REMOVED***
      this.defaultValue = void 0;
      return void 0;
    ***REMOVED***
    this.defaultValue = val;
    return this.defaultValue;
  ***REMOVED*** else if (arguments.length > 1) ***REMOVED***
    this.defaultValue = utils.args(arguments);
  ***REMOVED***
  return this.defaultValue;
***REMOVED***;

/**
 * Declares the index options for this schematype.
 *
 * ####Example:
 *
 *     var s = new Schema(***REMOVED*** name: ***REMOVED*** type: String, index: true ***REMOVED***)
 *     var s = new Schema(***REMOVED*** loc: ***REMOVED*** type: [Number], index: 'hashed' ***REMOVED***)
 *     var s = new Schema(***REMOVED*** loc: ***REMOVED*** type: [Number], index: '2d', sparse: true ***REMOVED***)
 *     var s = new Schema(***REMOVED*** loc: ***REMOVED*** type: [Number], index: ***REMOVED*** type: '2dsphere', sparse: true ***REMOVED******REMOVED***)
 *     var s = new Schema(***REMOVED*** date: ***REMOVED*** type: Date, index: ***REMOVED*** unique: true, expires: '1d' ***REMOVED******REMOVED***)
 *     Schema.path('my.path').index(true);
 *     Schema.path('my.date').index(***REMOVED*** expires: 60 ***REMOVED***);
 *     Schema.path('my.path').index(***REMOVED*** unique: true, sparse: true ***REMOVED***);
 *
 * ####NOTE:
 *
 * _Indexes are created in the background by default. Specify `background: false` to override._
 *
 * [Direction doesn't matter for single key indexes](http://www.mongodb.org/display/DOCS/Indexes#Indexes-CompoundKeysIndexes)
 *
 * @param ***REMOVED***Object|Boolean|String***REMOVED*** options
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @api public
 */

SchemaType.prototype.index = function(options) ***REMOVED***
  this._index = options;
  utils.expires(this._index);
  return this;
***REMOVED***;

/**
 * Declares an unique index.
 *
 * ####Example:
 *
 *     var s = new Schema(***REMOVED*** name: ***REMOVED*** type: String, unique: true ***REMOVED******REMOVED***);
 *     Schema.path('name').index(***REMOVED*** unique: true ***REMOVED***);
 *
 * _NOTE: violating the constraint returns an `E11000` error from MongoDB when saving, not a Mongoose validation error._
 *
 * @param ***REMOVED***Boolean***REMOVED*** bool
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @api public
 */

SchemaType.prototype.unique = function(bool) ***REMOVED***
  if (this._index === false) ***REMOVED***
    if (!bool) ***REMOVED***
      return;
    ***REMOVED***
    throw new Error('Path "' + this.path + '" may not have `index` set to ' +
      'false and `unique` set to true');
  ***REMOVED***
  if (this._index == null || this._index === true) ***REMOVED***
    this._index = ***REMOVED******REMOVED***;
  ***REMOVED*** else if (typeof this._index === 'string') ***REMOVED***
    this._index = ***REMOVED***type: this._index***REMOVED***;
  ***REMOVED***

  this._index.unique = bool;
  return this;
***REMOVED***;

/**
 * Declares a full text index.
 *
 * ###Example:
 *
 *      var s = new Schema(***REMOVED***name : ***REMOVED***type: String, text : true ***REMOVED***)
 *      Schema.path('name').index(***REMOVED***text : true***REMOVED***);
 * @param ***REMOVED***Boolean***REMOVED*** bool
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @api public
 */

SchemaType.prototype.text = function(bool) ***REMOVED***
  if (this._index === null || this._index === undefined ||
    typeof this._index === 'boolean') ***REMOVED***
    this._index = ***REMOVED******REMOVED***;
  ***REMOVED*** else if (typeof this._index === 'string') ***REMOVED***
    this._index = ***REMOVED***type: this._index***REMOVED***;
  ***REMOVED***

  this._index.text = bool;
  return this;
***REMOVED***;

/**
 * Declares a sparse index.
 *
 * ####Example:
 *
 *     var s = new Schema(***REMOVED*** name: ***REMOVED*** type: String, sparse: true ***REMOVED***)
 *     Schema.path('name').index(***REMOVED*** sparse: true ***REMOVED***);
 *
 * @param ***REMOVED***Boolean***REMOVED*** bool
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @api public
 */

SchemaType.prototype.sparse = function(bool) ***REMOVED***
  if (this._index === null || this._index === undefined ||
    typeof this._index === 'boolean') ***REMOVED***
    this._index = ***REMOVED******REMOVED***;
  ***REMOVED*** else if (typeof this._index === 'string') ***REMOVED***
    this._index = ***REMOVED***type: this._index***REMOVED***;
  ***REMOVED***

  this._index.sparse = bool;
  return this;
***REMOVED***;

/**
 * Adds a setter to this schematype.
 *
 * ####Example:
 *
 *     function capitalize (val) ***REMOVED***
 *       if (typeof val !== 'string') val = '';
 *       return val.charAt(0).toUpperCase() + val.substring(1);
 *     ***REMOVED***
 *
 *     // defining within the schema
 *     var s = new Schema(***REMOVED*** name: ***REMOVED*** type: String, set: capitalize ***REMOVED******REMOVED***);
 *
 *     // or with the SchemaType
 *     var s = new Schema(***REMOVED*** name: String ***REMOVED***)
 *     s.path('name').set(capitalize);
 *
 * Setters allow you to transform the data before it gets to the raw mongodb
 * document or query.
 *
 * Suppose you are implementing user registration for a website. Users provide
 * an email and password, which gets saved to mongodb. The email is a string
 * that you will want to normalize to lower case, in order to avoid one email
 * having more than one account -- e.g., otherwise, avenue@q.com can be registered for 2 accounts via avenue@q.com and AvEnUe@Q.CoM.
 *
 * You can set up email lower case normalization easily via a Mongoose setter.
 *
 *     function toLower(v) ***REMOVED***
 *       return v.toLowerCase();
 *     ***REMOVED***
 *
 *     var UserSchema = new Schema(***REMOVED***
 *       email: ***REMOVED*** type: String, set: toLower ***REMOVED***
 *     ***REMOVED***);
 *
 *     var User = db.model('User', UserSchema);
 *
 *     var user = new User(***REMOVED***email: 'AVENUE@Q.COM'***REMOVED***);
 *     console.log(user.email); // 'avenue@q.com'
 *
 *     // or
 *     var user = new User();
 *     user.email = 'Avenue@Q.com';
 *     console.log(user.email); // 'avenue@q.com'
 *     User.updateOne(***REMOVED*** _id: _id ***REMOVED***, ***REMOVED*** $set: ***REMOVED*** email: 'AVENUE@Q.COM' ***REMOVED*** ***REMOVED***); // update to 'avenue@q.com'
 *
 * As you can see above, setters allow you to transform the data before it
 * stored in MongoDB, or before executing a query.
 *
 * _NOTE: we could have also just used the built-in `lowercase: true` SchemaType option instead of defining our own function._
 *
 *     new Schema(***REMOVED*** email: ***REMOVED*** type: String, lowercase: true ***REMOVED******REMOVED***)
 *
 * Setters are also passed a second argument, the schematype on which the setter was defined. This allows for tailored behavior based on options passed in the schema.
 *
 *     function inspector (val, schematype) ***REMOVED***
 *       if (schematype.options.required) ***REMOVED***
 *         return schematype.path + ' is required';
 *       ***REMOVED*** else ***REMOVED***
 *         return val;
 *       ***REMOVED***
 *     ***REMOVED***
 *
 *     var VirusSchema = new Schema(***REMOVED***
 *       name: ***REMOVED*** type: String, required: true, set: inspector ***REMOVED***,
 *       taxonomy: ***REMOVED*** type: String, set: inspector ***REMOVED***
 *     ***REMOVED***)
 *
 *     var Virus = db.model('Virus', VirusSchema);
 *     var v = new Virus(***REMOVED*** name: 'Parvoviridae', taxonomy: 'Parvovirinae' ***REMOVED***);
 *
 *     console.log(v.name);     // name is required
 *     console.log(v.taxonomy); // Parvovirinae
 *
 * You can also use setters to modify other properties on the document. If
 * you're setting a property `name` on a document, the setter will run with
 * `this` as the document. Be careful, in mongoose 5 setters will also run
 * when querying by `name` with `this` as the query.
 *
 * ```javascript
 * const nameSchema = new Schema(***REMOVED*** name: String, keywords: [String] ***REMOVED***);
 * nameSchema.path('name').set(function(v) ***REMOVED***
 *   // Need to check if `this` is a document, because in mongoose 5
 *   // setters will also run on queries, in which case `this` will be a
 *   // mongoose query object.
 *   if (this instanceof Document && v != null) ***REMOVED***
 *     this.keywords = v.split(' ');
 *   ***REMOVED***
 *   return v;
 * ***REMOVED***);
 * ```
 *
 * @param ***REMOVED***Function***REMOVED*** fn
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @api public
 */

SchemaType.prototype.set = function(fn) ***REMOVED***
  if (typeof fn !== 'function') ***REMOVED***
    throw new TypeError('A setter must be a function.');
  ***REMOVED***
  this.setters.push(fn);
  return this;
***REMOVED***;

/**
 * Adds a getter to this schematype.
 *
 * ####Example:
 *
 *     function dob (val) ***REMOVED***
 *       if (!val) return val;
 *       return (val.getMonth() + 1) + "/" + val.getDate() + "/" + val.getFullYear();
 *     ***REMOVED***
 *
 *     // defining within the schema
 *     var s = new Schema(***REMOVED*** born: ***REMOVED*** type: Date, get: dob ***REMOVED***)
 *
 *     // or by retreiving its SchemaType
 *     var s = new Schema(***REMOVED*** born: Date ***REMOVED***)
 *     s.path('born').get(dob)
 *
 * Getters allow you to transform the representation of the data as it travels from the raw mongodb document to the value that you see.
 *
 * Suppose you are storing credit card numbers and you want to hide everything except the last 4 digits to the mongoose user. You can do so by defining a getter in the following way:
 *
 *     function obfuscate (cc) ***REMOVED***
 *       return '****-****-****-' + cc.slice(cc.length-4, cc.length);
 *     ***REMOVED***
 *
 *     var AccountSchema = new Schema(***REMOVED***
 *       creditCardNumber: ***REMOVED*** type: String, get: obfuscate ***REMOVED***
 *     ***REMOVED***);
 *
 *     var Account = db.model('Account', AccountSchema);
 *
 *     Account.findById(id, function (err, found) ***REMOVED***
 *       console.log(found.creditCardNumber); // '****-****-****-1234'
 *     ***REMOVED***);
 *
 * Getters are also passed a second argument, the schematype on which the getter was defined. This allows for tailored behavior based on options passed in the schema.
 *
 *     function inspector (val, schematype) ***REMOVED***
 *       if (schematype.options.required) ***REMOVED***
 *         return schematype.path + ' is required';
 *       ***REMOVED*** else ***REMOVED***
 *         return schematype.path + ' is not';
 *       ***REMOVED***
 *     ***REMOVED***
 *
 *     var VirusSchema = new Schema(***REMOVED***
 *       name: ***REMOVED*** type: String, required: true, get: inspector ***REMOVED***,
 *       taxonomy: ***REMOVED*** type: String, get: inspector ***REMOVED***
 *     ***REMOVED***)
 *
 *     var Virus = db.model('Virus', VirusSchema);
 *
 *     Virus.findById(id, function (err, virus) ***REMOVED***
 *       console.log(virus.name);     // name is required
 *       console.log(virus.taxonomy); // taxonomy is not
 *     ***REMOVED***)
 *
 * @param ***REMOVED***Function***REMOVED*** fn
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @api public
 */

SchemaType.prototype.get = function(fn) ***REMOVED***
  if (typeof fn !== 'function') ***REMOVED***
    throw new TypeError('A getter must be a function.');
  ***REMOVED***
  this.getters.push(fn);
  return this;
***REMOVED***;

/**
 * Adds validator(s) for this document path.
 *
 * Validators always receive the value to validate as their first argument and must return `Boolean`. Returning `false` means validation failed.
 *
 * The error message argument is optional. If not passed, the [default generic error message template](#error_messages_MongooseError-messages) will be used.
 *
 * ####Examples:
 *
 *     // make sure every value is equal to "something"
 *     function validator (val) ***REMOVED***
 *       return val == 'something';
 *     ***REMOVED***
 *     new Schema(***REMOVED*** name: ***REMOVED*** type: String, validate: validator ***REMOVED******REMOVED***);
 *
 *     // with a custom error message
 *
 *     var custom = [validator, 'Uh oh, ***REMOVED***PATH***REMOVED*** does not equal "something".']
 *     new Schema(***REMOVED*** name: ***REMOVED*** type: String, validate: custom ***REMOVED******REMOVED***);
 *
 *     // adding many validators at a time
 *
 *     var many = [
 *         ***REMOVED*** validator: validator, msg: 'uh oh' ***REMOVED***
 *       , ***REMOVED*** validator: anotherValidator, msg: 'failed' ***REMOVED***
 *     ]
 *     new Schema(***REMOVED*** name: ***REMOVED*** type: String, validate: many ***REMOVED******REMOVED***);
 *
 *     // or utilizing SchemaType methods directly:
 *
 *     var schema = new Schema(***REMOVED*** name: 'string' ***REMOVED***);
 *     schema.path('name').validate(validator, 'validation of `***REMOVED***PATH***REMOVED***` failed with value `***REMOVED***VALUE***REMOVED***`');
 *
 * ####Error message templates:
 *
 * From the examples above, you may have noticed that error messages support basic templating. There are a few other template keywords besides `***REMOVED***PATH***REMOVED***` and `***REMOVED***VALUE***REMOVED***` too. To find out more, details are available [here](#error_messages_MongooseError.messages)
 *
 * ####Asynchronous validation:
 *
 * Passing a validator function that receives two arguments tells mongoose that the validator is an asynchronous validator. The first argument passed to the validator function is the value being validated. The second argument is a callback function that must called when you finish validating the value and passed either `true` or `false` to communicate either success or failure respectively.
 *
 *     schema.path('name').validate(***REMOVED***
 *       isAsync: true,
 *       validator: function (value, respond) ***REMOVED***
 *         doStuff(value, function () ***REMOVED***
 *           ...
 *           respond(false); // validation failed
 *         ***REMOVED***);
 *       ***REMOVED***,
 *       message: 'Custom error message!' // Optional
 *     ***REMOVED***);
 *
 *     // Can also return a promise
 *     schema.path('name').validate(***REMOVED***
 *       validator: function (value) ***REMOVED***
 *         return new Promise(function (resolve, reject) ***REMOVED***
 *           resolve(false); // validation failed
 *         ***REMOVED***);
 *       ***REMOVED***
 *     ***REMOVED***);
 *
 * You might use asynchronous validators to retreive other documents from the database to validate against or to meet other I/O bound validation needs.
 *
 * Validation occurs `pre('save')` or whenever you manually execute [document#validate](#document_Document-validate).
 *
 * If validation fails during `pre('save')` and no callback was passed to receive the error, an `error` event will be emitted on your Models associated db [connection](#connection_Connection), passing the validation error object along.
 *
 *     var conn = mongoose.createConnection(..);
 *     conn.on('error', handleError);
 *
 *     var Product = conn.model('Product', yourSchema);
 *     var dvd = new Product(..);
 *     dvd.save(); // emits error on the `conn` above
 *
 * If you desire handling these errors at the Model level, attach an `error` listener to your Model and the event will instead be emitted there.
 *
 *     // registering an error listener on the Model lets us handle errors more locally
 *     Product.on('error', handleError);
 *
 * @param ***REMOVED***RegExp|Function|Object***REMOVED*** obj validator
 * @param ***REMOVED***String***REMOVED*** [errorMsg] optional error message
 * @param ***REMOVED***String***REMOVED*** [type] optional validator type
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @api public
 */

SchemaType.prototype.validate = function(obj, message, type) ***REMOVED***
  if (typeof obj === 'function' || obj && utils.getFunctionName(obj.constructor) === 'RegExp') ***REMOVED***
    var properties;
    if (message instanceof Object && !type) ***REMOVED***
      properties = utils.clone(message);
      if (!properties.message) ***REMOVED***
        properties.message = properties.msg;
      ***REMOVED***
      properties.validator = obj;
      properties.type = properties.type || 'user defined';
    ***REMOVED*** else ***REMOVED***
      if (!message) ***REMOVED***
        message = MongooseError.messages.general.default;
      ***REMOVED***
      if (!type) ***REMOVED***
        type = 'user defined';
      ***REMOVED***
      properties = ***REMOVED***message: message, type: type, validator: obj***REMOVED***;
    ***REMOVED***
    this.validators.push(properties);
    return this;
  ***REMOVED***

  var i,
      length,
      arg;

  for (i = 0, length = arguments.length; i < length; i++) ***REMOVED***
    arg = arguments[i];
    if (!(arg && utils.getFunctionName(arg.constructor) === 'Object')) ***REMOVED***
      var msg = 'Invalid validator. Received (' + typeof arg + ') '
          + arg
          + '. See http://mongoosejs.com/docs/api.html#schematype_SchemaType-validate';

      throw new Error(msg);
    ***REMOVED***
    this.validate(arg.validator, arg);
  ***REMOVED***

  return this;
***REMOVED***;

/**
 * Adds a required validator to this SchemaType. The validator gets added
 * to the front of this SchemaType's validators array using `unshift()`.
 *
 * ####Example:
 *
 *     var s = new Schema(***REMOVED*** born: ***REMOVED*** type: Date, required: true ***REMOVED***)
 *
 *     // or with custom error message
 *
 *     var s = new Schema(***REMOVED*** born: ***REMOVED*** type: Date, required: '***REMOVED***PATH***REMOVED*** is required!' ***REMOVED***)
 *
 *     // or with a function
 *
 *     var s = new Schema(***REMOVED***
 *       userId: ObjectId,
 *       username: ***REMOVED***
 *         type: String,
 *         required: function() ***REMOVED*** return this.userId != null; ***REMOVED***
 *       ***REMOVED***
 *     ***REMOVED***)
 *
 *     // or with a function and a custom message
 *     var s = new Schema(***REMOVED***
 *       userId: ObjectId,
 *       username: ***REMOVED***
 *         type: String,
 *         required: [
 *           function() ***REMOVED*** return this.userId != null; ***REMOVED***,
 *           'username is required if id is specified'
 *         ]
 *       ***REMOVED***
 *     ***REMOVED***)
 *
 *     // or through the path API
 *
 *     Schema.path('name').required(true);
 *
 *     // with custom error messaging
 *
 *     Schema.path('name').required(true, 'grrr :( ');
 *
 *     // or make a path conditionally required based on a function
 *     var isOver18 = function() ***REMOVED*** return this.age >= 18; ***REMOVED***;
 *     Schema.path('voterRegistrationId').required(isOver18);
 *
 * The required validator uses the SchemaType's `checkRequired` function to
 * determine whether a given value satisfies the required validator. By default,
 * a value satisfies the required validator if `val != null` (that is, if
 * the value is not null nor undefined). However, most built-in mongoose schema
 * types override the default `checkRequired` function:
 *
 * @param ***REMOVED***Boolean|Function|Object***REMOVED*** required enable/disable the validator, or function that returns required boolean, or options object
 * @param ***REMOVED***Boolean|Function***REMOVED*** [options.isRequired] enable/disable the validator, or function that returns required boolean
 * @param ***REMOVED***Function***REMOVED*** [options.ErrorConstructor] custom error constructor. The constructor receives 1 parameter, an object containing the validator properties.
 * @param ***REMOVED***String***REMOVED*** [message] optional custom error message
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @see Customized Error Messages #error_messages_MongooseError-messages
 * @see SchemaArray#checkRequired #schema_array_SchemaArray.checkRequired
 * @see SchemaBoolean#checkRequired #schema_boolean_SchemaBoolean-checkRequired
 * @see SchemaBuffer#checkRequired #schema_buffer_SchemaBuffer.schemaName
 * @see SchemaNumber#checkRequired #schema_number_SchemaNumber-min
 * @see SchemaObjectId#checkRequired #schema_objectid_ObjectId-auto
 * @see SchemaString#checkRequired #schema_string_SchemaString-checkRequired
 * @api public
 */

SchemaType.prototype.required = function(required, message) ***REMOVED***
  var customOptions = ***REMOVED******REMOVED***;
  if (typeof required === 'object') ***REMOVED***
    customOptions = required;
    message = customOptions.message || message;
    required = required.isRequired;
  ***REMOVED***

  if (required === false) ***REMOVED***
    this.validators = this.validators.filter(function(v) ***REMOVED***
      return v.validator !== this.requiredValidator;
    ***REMOVED***, this);

    this.isRequired = false;
    return this;
  ***REMOVED***

  var _this = this;
  this.isRequired = true;

  this.requiredValidator = function(v) ***REMOVED***
    // no validation when this path wasn't selected in the query.
    if (this != null && this.$__ && !this.isSelected(_this.path) && !this.isModified(_this.path)) ***REMOVED***
      return true;
    ***REMOVED***

    return ((typeof required === 'function') && !required.apply(this)) ||
        _this.checkRequired(v, this);
  ***REMOVED***;
  this.originalRequiredValue = required;

  if (typeof required === 'string') ***REMOVED***
    message = required;
    required = undefined;
  ***REMOVED***

  var msg = message || MongooseError.messages.general.required;
  this.validators.unshift(Object.assign(***REMOVED******REMOVED***, customOptions, ***REMOVED***
    validator: this.requiredValidator,
    message: msg,
    type: 'required'
  ***REMOVED***));

  return this;
***REMOVED***;

/**
 * Gets the default value
 *
 * @param ***REMOVED***Object***REMOVED*** scope the scope which callback are executed
 * @param ***REMOVED***Boolean***REMOVED*** init
 * @api private
 */

SchemaType.prototype.getDefault = function(scope, init) ***REMOVED***
  var ret = typeof this.defaultValue === 'function'
    ? this.defaultValue.call(scope)
    : this.defaultValue;

  if (ret !== null && ret !== undefined) ***REMOVED***
    if (typeof ret === 'object' && (!this.options || !this.options.shared)) ***REMOVED***
      ret = utils.clone(ret);
    ***REMOVED***

    var casted = this.cast(ret, scope, init);
    if (casted && casted.$isSingleNested) ***REMOVED***
      casted.$parent = scope;
    ***REMOVED***
    return casted;
  ***REMOVED***
  return ret;
***REMOVED***;

/*!
 * Applies setters without casting
 *
 * @api private
 */

SchemaType.prototype._applySetters = function(value, scope, init, priorVal) ***REMOVED***
  var v = value;
  var setters = this.setters;
  var len = setters.length;
  var caster = this.caster;

  while (len--) ***REMOVED***
    v = setters[len].call(scope, v, this);
  ***REMOVED***

  if (Array.isArray(v) && caster && caster.setters) ***REMOVED***
    var newVal = [];
    for (var i = 0; i < v.length; i++) ***REMOVED***
      newVal.push(caster.applySetters(v[i], scope, init, priorVal));
    ***REMOVED***
    v = newVal;
  ***REMOVED***

  return v;
***REMOVED***;

/**
 * Applies setters
 *
 * @param ***REMOVED***Object***REMOVED*** value
 * @param ***REMOVED***Object***REMOVED*** scope
 * @param ***REMOVED***Boolean***REMOVED*** init
 * @api private
 */

SchemaType.prototype.applySetters = function(value, scope, init, priorVal, options) ***REMOVED***
  var v = this._applySetters(value, scope, init, priorVal, options);

  if (v == null) ***REMOVED***
    return v;
  ***REMOVED***

  // do not cast until all setters are applied #665
  v = this.cast(v, scope, init, priorVal, options);

  return v;
***REMOVED***;

/**
 * Applies getters to a value
 *
 * @param ***REMOVED***Object***REMOVED*** value
 * @param ***REMOVED***Object***REMOVED*** scope
 * @api private
 */

SchemaType.prototype.applyGetters = function(value, scope) ***REMOVED***
  let v = value;
  const getters = this.getters;
  const len = getters.length;

  if (len === 0) ***REMOVED***
    return v;
  ***REMOVED***

  for (let i = 0; i < len; ++i) ***REMOVED***
    v = getters[i].call(scope, v, this);
  ***REMOVED***

  return v;
***REMOVED***;

/**
 * Sets default `select()` behavior for this path.
 *
 * Set to `true` if this path should always be included in the results, `false` if it should be excluded by default. This setting can be overridden at the query level.
 *
 * ####Example:
 *
 *     T = db.model('T', new Schema(***REMOVED*** x: ***REMOVED*** type: String, select: true ***REMOVED******REMOVED***));
 *     T.find(..); // field x will always be selected ..
 *     // .. unless overridden;
 *     T.find().select('-x').exec(callback);
 *
 * @param ***REMOVED***Boolean***REMOVED*** val
 * @return ***REMOVED***SchemaType***REMOVED*** this
 * @api public
 */

SchemaType.prototype.select = function select(val) ***REMOVED***
  this.selected = !!val;
  return this;
***REMOVED***;

/**
 * Performs a validation of `value` using the validators declared for this SchemaType.
 *
 * @param ***REMOVED***any***REMOVED*** value
 * @param ***REMOVED***Function***REMOVED*** callback
 * @param ***REMOVED***Object***REMOVED*** scope
 * @api private
 */

SchemaType.prototype.doValidate = function(value, fn, scope) ***REMOVED***
  var err = false;
  var path = this.path;
  var count = this.validators.length;

  if (!count) ***REMOVED***
    return fn(null);
  ***REMOVED***

  var validate = function(ok, validatorProperties) ***REMOVED***
    if (err) ***REMOVED***
      return;
    ***REMOVED***
    if (ok === undefined || ok) ***REMOVED***
      if (--count <= 0) ***REMOVED***
        utils.immediate(function() ***REMOVED***
          fn(null);
        ***REMOVED***);
      ***REMOVED***
    ***REMOVED*** else ***REMOVED***
      var ErrorConstructor = validatorProperties.ErrorConstructor || ValidatorError;
      err = new ErrorConstructor(validatorProperties);
      err.$isValidatorError = true;
      utils.immediate(function() ***REMOVED***
        fn(err);
      ***REMOVED***);
    ***REMOVED***
  ***REMOVED***;

  var _this = this;
  this.validators.forEach(function(v) ***REMOVED***
    if (err) ***REMOVED***
      return;
    ***REMOVED***

    var validator = v.validator;
    var ok;

    var validatorProperties = utils.clone(v);
    validatorProperties.path = path;
    validatorProperties.value = value;

    if (validator instanceof RegExp) ***REMOVED***
      validate(validator.test(value), validatorProperties);
    ***REMOVED*** else if (typeof validator === 'function') ***REMOVED***
      if (value === undefined && validator !== _this.requiredValidator) ***REMOVED***
        validate(true, validatorProperties);
        return;
      ***REMOVED***
      if (validatorProperties.isAsync) ***REMOVED***
        asyncValidate(validator, scope, value, validatorProperties, validate);
      ***REMOVED*** else ***REMOVED***
        try ***REMOVED***
          ok = validator.call(scope, value);
        ***REMOVED*** catch (error) ***REMOVED***
          ok = false;
          validatorProperties.reason = error;
          if (error.message) ***REMOVED***
            validatorProperties.message = error.message;
          ***REMOVED***
        ***REMOVED***
        if (ok != null && typeof ok.then === 'function') ***REMOVED***
          ok.then(
            function(ok) ***REMOVED*** validate(ok, validatorProperties); ***REMOVED***,
            function(error) ***REMOVED***
              validatorProperties.reason = error;
              ok = false;
              validate(ok, validatorProperties);
            ***REMOVED***);
        ***REMOVED*** else ***REMOVED***
          validate(ok, validatorProperties);
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***);
***REMOVED***;

/*!
 * Handle async validators
 */

function asyncValidate(validator, scope, value, props, cb) ***REMOVED***
  var called = false;
  var returnVal = validator.call(scope, value, function(ok, customMsg) ***REMOVED***
    if (called) ***REMOVED***
      return;
    ***REMOVED***
    called = true;
    if (typeof returnVal === 'boolean') ***REMOVED***
      return;
    ***REMOVED***
    if (customMsg) ***REMOVED***
      props.message = customMsg;
    ***REMOVED***
    cb(ok, props);
  ***REMOVED***);
  if (typeof returnVal === 'boolean') ***REMOVED***
    called = true;
    cb(returnVal, props);
  ***REMOVED*** else if (returnVal && typeof returnVal.then === 'function') ***REMOVED***
    // Promise
    returnVal.then(
      function(ok) ***REMOVED***
        if (called) ***REMOVED***
          return;
        ***REMOVED***
        called = true;
        cb(ok, props);
      ***REMOVED***,
      function(error) ***REMOVED***
        if (called) ***REMOVED***
          return;
        ***REMOVED***
        called = true;

        props.reason = error;
        cb(false, props);
      ***REMOVED***);
  ***REMOVED***
***REMOVED***

/**
 * Performs a validation of `value` using the validators declared for this SchemaType.
 *
 * ####Note:
 *
 * This method ignores the asynchronous validators.
 *
 * @param ***REMOVED***any***REMOVED*** value
 * @param ***REMOVED***Object***REMOVED*** scope
 * @return ***REMOVED***MongooseError|undefined***REMOVED***
 * @api private
 */

SchemaType.prototype.doValidateSync = function(value, scope) ***REMOVED***
  var err = null,
      path = this.path,
      count = this.validators.length;

  if (!count) ***REMOVED***
    return null;
  ***REMOVED***

  var validate = function(ok, validatorProperties) ***REMOVED***
    if (err) ***REMOVED***
      return;
    ***REMOVED***
    if (ok !== undefined && !ok) ***REMOVED***
      var ErrorConstructor = validatorProperties.ErrorConstructor || ValidatorError;
      err = new ErrorConstructor(validatorProperties);
      err.$isValidatorError = true;
    ***REMOVED***
  ***REMOVED***;

  var validators = this.validators;
  if (value === void 0) ***REMOVED***
    if (this.validators.length > 0 && this.validators[0].type === 'required') ***REMOVED***
      validators = [this.validators[0]];
    ***REMOVED*** else ***REMOVED***
      return null;
    ***REMOVED***
  ***REMOVED***

  validators.forEach(function(v) ***REMOVED***
    if (err) ***REMOVED***
      return;
    ***REMOVED***

    var validator = v.validator;
    var validatorProperties = utils.clone(v);
    validatorProperties.path = path;
    validatorProperties.value = value;
    var ok;

    if (validator instanceof RegExp) ***REMOVED***
      validate(validator.test(value), validatorProperties);
    ***REMOVED*** else if (typeof validator === 'function') ***REMOVED***
      // if not async validators
      if (validator.length !== 2 && !validatorProperties.isAsync) ***REMOVED***
        try ***REMOVED***
          ok = validator.call(scope, value);
        ***REMOVED*** catch (error) ***REMOVED***
          ok = false;
          validatorProperties.reason = error;
        ***REMOVED***
        validate(ok, validatorProperties);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***);

  return err;
***REMOVED***;

/**
 * Determines if value is a valid Reference.
 *
 * @param ***REMOVED***SchemaType***REMOVED*** self
 * @param ***REMOVED***Object***REMOVED*** value
 * @param ***REMOVED***Document***REMOVED*** doc
 * @param ***REMOVED***Boolean***REMOVED*** init
 * @return ***REMOVED***Boolean***REMOVED***
 * @api private
 */

SchemaType._isRef = function(self, value, doc, init) ***REMOVED***
  // fast path
  var ref = init && self.options && self.options.ref;

  if (!ref && doc && doc.$__fullPath) ***REMOVED***
    // checks for
    // - this populated with adhoc model and no ref was set in schema OR
    // - setting / pushing values after population
    var path = doc.$__fullPath(self.path);
    var owner = doc.ownerDocument ? doc.ownerDocument() : doc;
    ref = owner.populated(path);
  ***REMOVED***

  if (ref) ***REMOVED***
    if (value == null) ***REMOVED***
      return true;
    ***REMOVED***
    if (!Buffer.isBuffer(value) && // buffers are objects too
        value._bsontype !== 'Binary' // raw binary value from the db
        && utils.isObject(value) // might have deselected _id in population query
    ) ***REMOVED***
      return true;
    ***REMOVED***
  ***REMOVED***

  return false;
***REMOVED***;

/*!
 * ignore
 */

function handleSingle(val) ***REMOVED***
  return this.castForQuery(val);
***REMOVED***

/*!
 * ignore
 */

function handleArray(val) ***REMOVED***
  var _this = this;
  if (!Array.isArray(val)) ***REMOVED***
    return [this.castForQuery(val)];
  ***REMOVED***
  return val.map(function(m) ***REMOVED***
    return _this.castForQuery(m);
  ***REMOVED***);
***REMOVED***

/*!
 * Just like handleArray, except also allows `[]` because surprisingly
 * `$in: [1, []]` works fine
 */

function handle$in(val) ***REMOVED***
  var _this = this;
  if (!Array.isArray(val)) ***REMOVED***
    return [this.castForQuery(val)];
  ***REMOVED***
  return val.map(function(m) ***REMOVED***
    if (Array.isArray(m) && m.length === 0) ***REMOVED***
      return m;
    ***REMOVED***
    return _this.castForQuery(m);
  ***REMOVED***);
***REMOVED***

/*!
 * ignore
 */

SchemaType.prototype.$conditionalHandlers = ***REMOVED***
  $all: handleArray,
  $eq: handleSingle,
  $in: handle$in,
  $ne: handleSingle,
  $nin: handleArray,
  $exists: $exists,
  $type: $type
***REMOVED***;

/*!
 * Wraps `castForQuery` to handle context
 */

SchemaType.prototype.castForQueryWrapper = function(params) ***REMOVED***
  this.$$context = params.context;
  if ('$conditional' in params) ***REMOVED***
    return this.castForQuery(params.$conditional, params.val);
  ***REMOVED***
  if (params.$skipQueryCastForUpdate) ***REMOVED***
    return this._castForQuery(params.val);
  ***REMOVED***
  return this.castForQuery(params.val);
***REMOVED***;

/**
 * Cast the given value with the given optional query operator.
 *
 * @param ***REMOVED***String***REMOVED*** [$conditional] query operator, like `$eq` or `$in`
 * @param ***REMOVED***any***REMOVED*** val
 * @api private
 */

SchemaType.prototype.castForQuery = function($conditional, val) ***REMOVED***
  var handler;
  if (arguments.length === 2) ***REMOVED***
    handler = this.$conditionalHandlers[$conditional];
    if (!handler) ***REMOVED***
      throw new Error('Can\'t use ' + $conditional);
    ***REMOVED***
    return handler.call(this, val);
  ***REMOVED***
  val = $conditional;
  return this._castForQuery(val);
***REMOVED***;

/*!
 * Internal switch for runSetters
 *
 * @api private
 */

SchemaType.prototype._castForQuery = function(val) ***REMOVED***
  return this.applySetters(val, this.$$context);
***REMOVED***;

/**
 * Default check for if this path satisfies the `required` validator.
 *
 * @param ***REMOVED***any***REMOVED*** val
 * @api private
 */

SchemaType.prototype.checkRequired = function(val) ***REMOVED***
  return val != null;
***REMOVED***;

/*!
 * Module exports.
 */

module.exports = exports = SchemaType;

exports.CastError = CastError;

exports.ValidatorError = ValidatorError;
