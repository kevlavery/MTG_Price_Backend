'use strict';

/*!
 * ignore
 */

module.exports = function selectPopulatedFields(query) ***REMOVED***
  var opts = query._mongooseOptions;

  if (opts.populate != null) ***REMOVED***
    var paths = Object.keys(opts.populate);
    var i;
    var userProvidedFields = query._userProvidedFields || ***REMOVED******REMOVED***;
    if (query.selectedInclusively()) ***REMOVED***
      for (i = 0; i < paths.length; ++i) ***REMOVED***
        if (!isPathInFields(userProvidedFields, paths[i])) ***REMOVED***
          query.select(paths[i]);
        ***REMOVED***
      ***REMOVED***
    ***REMOVED*** else if (query.selectedExclusively()) ***REMOVED***
      for (i = 0; i < paths.length; ++i) ***REMOVED***
        if (userProvidedFields[paths[i]] == null) ***REMOVED***
          delete query._fields[paths[i]];
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
***REMOVED***;

/*!
 * ignore
 */

function isPathInFields(userProvidedFields, path) ***REMOVED***
  var pieces = path.split('.');
  var len = pieces.length;
  var cur = pieces[0];
  for (var i = 1; i < len; ++i) ***REMOVED***
    if (userProvidedFields[cur] != null) ***REMOVED***
      return true;
    ***REMOVED***
    cur += '.' + pieces[i];
  ***REMOVED***
  return userProvidedFields[cur] != null;
***REMOVED***
