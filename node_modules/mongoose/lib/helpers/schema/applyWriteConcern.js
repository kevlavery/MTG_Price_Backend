'use strict';

const get = require('lodash.get');

module.exports = function applyWriteConcern(schema, options) ***REMOVED***
  const writeConcern = get(schema, 'options.writeConcern', ***REMOVED******REMOVED***);
  if (!('w' in options) && writeConcern.w != null) ***REMOVED***
    options.w = writeConcern.w;
  ***REMOVED***
  if (!('j' in options) && writeConcern.j != null) ***REMOVED***
    options.j = writeConcern.j;
  ***REMOVED***
  if (!('wtimeout' in options) && writeConcern.wtimeout != null) ***REMOVED***
    options.wtimeout = writeConcern.wtimeout;
  ***REMOVED***
***REMOVED***;
