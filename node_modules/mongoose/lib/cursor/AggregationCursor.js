/*!
 * Module dependencies.
 */

var Readable = require('stream').Readable;
var eachAsync = require('../services/cursor/eachAsync');
var util = require('util');
var utils = require('../utils');

/**
 * An AggregationCursor is a concurrency primitive for processing aggregation
 * results one document at a time. It is analogous to QueryCursor.
 *
 * An AggregationCursor fulfills the Node.js streams3 API,
 * in addition to several other mechanisms for loading documents from MongoDB
 * one at a time.
 *
 * Creating an AggregationCursor executes the model's pre aggregate hooks,
 * but **not** the model's post aggregate hooks.
 *
 * Unless you're an advanced user, do **not** instantiate this class directly.
 * Use [`Aggregate#cursor()`](/docs/api.html#aggregate_Aggregate-cursor) instead.
 *
 * @param ***REMOVED***Aggregate***REMOVED*** agg
 * @param ***REMOVED***Object***REMOVED*** options
 * @inherits Readable
 * @event `cursor`: Emitted when the cursor is created
 * @event `error`: Emitted when an error occurred
 * @event `data`: Emitted when the stream is flowing and the next doc is ready
 * @event `end`: Emitted when the stream is exhausted
 * @api public
 */

function AggregationCursor(agg) ***REMOVED***
  Readable.call(this, ***REMOVED*** objectMode: true ***REMOVED***);

  this.cursor = null;
  this.agg = agg;
  this._transforms = [];
  var model = agg._model;
  delete agg.options.cursor.useMongooseAggCursor;

  _init(model, this, agg);
***REMOVED***

util.inherits(AggregationCursor, Readable);

/*!
 * ignore
 */

function _init(model, c, agg) ***REMOVED***
  if (!model.collection.buffer) ***REMOVED***
    model.hooks.execPre('aggregate', agg, function() ***REMOVED***
      c.cursor = model.collection.aggregate(agg._pipeline, agg.options || ***REMOVED******REMOVED***);
      c.emit('cursor', c.cursor);
    ***REMOVED***);
  ***REMOVED*** else ***REMOVED***
    model.collection.emitter.once('queue', function() ***REMOVED***
      model.hooks.execPre('aggregate', agg, function() ***REMOVED***
        c.cursor = model.collection.aggregate(agg._pipeline, agg.options || ***REMOVED******REMOVED***);
        c.emit('cursor', c.cursor);
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***
***REMOVED***

/*!
 * Necessary to satisfy the Readable API
 */

AggregationCursor.prototype._read = function() ***REMOVED***
  var _this = this;
  _next(this, function(error, doc) ***REMOVED***
    if (error) ***REMOVED***
      return _this.emit('error', error);
    ***REMOVED***
    if (!doc) ***REMOVED***
      _this.push(null);
      _this.cursor.close(function(error) ***REMOVED***
        if (error) ***REMOVED***
          return _this.emit('error', error);
        ***REMOVED***
        setTimeout(function() ***REMOVED***
          _this.emit('close');
        ***REMOVED***, 0);
      ***REMOVED***);
      return;
    ***REMOVED***
    _this.push(doc);
  ***REMOVED***);
***REMOVED***;

/**
 * Registers a transform function which subsequently maps documents retrieved
 * via the streams interface or `.next()`
 *
 * ####Example
 *
 *     // Map documents returned by `data` events
 *     Thing.
 *       find(***REMOVED*** name: /^hello/ ***REMOVED***).
 *       cursor().
 *       map(function (doc) ***REMOVED***
 *        doc.foo = "bar";
 *        return doc;
 *       ***REMOVED***)
 *       on('data', function(doc) ***REMOVED*** console.log(doc.foo); ***REMOVED***);
 *
 *     // Or map documents returned by `.next()`
 *     var cursor = Thing.find(***REMOVED*** name: /^hello/ ***REMOVED***).
 *       cursor().
 *       map(function (doc) ***REMOVED***
 *         doc.foo = "bar";
 *         return doc;
 *       ***REMOVED***);
 *     cursor.next(function(error, doc) ***REMOVED***
 *       console.log(doc.foo);
 *     ***REMOVED***);
 *
 * @param ***REMOVED***Function***REMOVED*** fn
 * @return ***REMOVED***QueryCursor***REMOVED***
 * @api public
 * @method map
 */

AggregationCursor.prototype.map = function(fn) ***REMOVED***
  this._transforms.push(fn);
  return this;
***REMOVED***;

/*!
 * Marks this cursor as errored
 */

AggregationCursor.prototype._markError = function(error) ***REMOVED***
  this._error = error;
  return this;
***REMOVED***;

/**
 * Marks this cursor as closed. Will stop streaming and subsequent calls to
 * `next()` will error.
 *
 * @param ***REMOVED***Function***REMOVED*** callback
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 * @method close
 * @emits close
 * @see MongoDB driver cursor#close http://mongodb.github.io/node-mongodb-native/2.1/api/Cursor.html#close
 */

AggregationCursor.prototype.close = function(callback) ***REMOVED***
  return utils.promiseOrCallback(callback, cb => ***REMOVED***
    this.cursor.close(error => ***REMOVED***
      if (error) ***REMOVED***
        cb(error);
        return this.listeners('error').length > 0 && this.emit('error', error);
      ***REMOVED***
      this.emit('close');
      cb(null);
    ***REMOVED***);
  ***REMOVED***);
***REMOVED***;

/**
 * Get the next document from this cursor. Will return `null` when there are
 * no documents left.
 *
 * @param ***REMOVED***Function***REMOVED*** callback
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 * @method next
 */

AggregationCursor.prototype.next = function(callback) ***REMOVED***
  return utils.promiseOrCallback(callback, cb => ***REMOVED***
    _next(this, cb);
  ***REMOVED***);
***REMOVED***;

/**
 * Execute `fn` for every document in the cursor. If `fn` returns a promise,
 * will wait for the promise to resolve before iterating on to the next one.
 * Returns a promise that resolves when done.
 *
 * @param ***REMOVED***Function***REMOVED*** fn
 * @param ***REMOVED***Object***REMOVED*** [options]
 * @param ***REMOVED***Number***REMOVED*** [options.parallel] the number of promises to execute in parallel. Defaults to 1.
 * @param ***REMOVED***Function***REMOVED*** [callback] executed when all docs have been processed
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 * @method eachAsync
 */

AggregationCursor.prototype.eachAsync = function(fn, opts, callback) ***REMOVED***
  var _this = this;
  if (typeof opts === 'function') ***REMOVED***
    callback = opts;
    opts = ***REMOVED******REMOVED***;
  ***REMOVED***
  opts = opts || ***REMOVED******REMOVED***;

  return eachAsync(function(cb) ***REMOVED*** return _next(_this, cb); ***REMOVED***, fn, opts, callback);
***REMOVED***;

/**
 * Adds a [cursor flag](http://mongodb.github.io/node-mongodb-native/2.2/api/Cursor.html#addCursorFlag).
 * Useful for setting the `noCursorTimeout` and `tailable` flags.
 *
 * @param ***REMOVED***String***REMOVED*** flag
 * @param ***REMOVED***Boolean***REMOVED*** value
 * @return ***REMOVED***AggregationCursor***REMOVED*** this
 * @api public
 * @method addCursorFlag
 */

AggregationCursor.prototype.addCursorFlag = function(flag, value) ***REMOVED***
  var _this = this;
  _waitForCursor(this, function() ***REMOVED***
    _this.cursor.addCursorFlag(flag, value);
  ***REMOVED***);
  return this;
***REMOVED***;

/*!
 * ignore
 */

function _waitForCursor(ctx, cb) ***REMOVED***
  if (ctx.cursor) ***REMOVED***
    return cb();
  ***REMOVED***
  ctx.once('cursor', function() ***REMOVED***
    cb();
  ***REMOVED***);
***REMOVED***

/*!
 * Get the next doc from the underlying cursor and mongooseify it
 * (populate, etc.)
 */

function _next(ctx, cb) ***REMOVED***
  var callback = cb;
  if (ctx._transforms.length) ***REMOVED***
    callback = function(err, doc) ***REMOVED***
      if (err || doc === null) ***REMOVED***
        return cb(err, doc);
      ***REMOVED***
      cb(err, ctx._transforms.reduce(function(doc, fn) ***REMOVED***
        return fn(doc);
      ***REMOVED***, doc));
    ***REMOVED***;
  ***REMOVED***

  if (ctx._error) ***REMOVED***
    return process.nextTick(function() ***REMOVED***
      callback(ctx._error);
    ***REMOVED***);
  ***REMOVED***

  if (ctx.cursor) ***REMOVED***
    return ctx.cursor.next(function(error, doc) ***REMOVED***
      if (error) ***REMOVED***
        return callback(error);
      ***REMOVED***
      if (!doc) ***REMOVED***
        return callback(null, null);
      ***REMOVED***

      callback(null, doc);
    ***REMOVED***);
  ***REMOVED*** else ***REMOVED***
    ctx.once('cursor', function() ***REMOVED***
      _next(ctx, cb);
    ***REMOVED***);
  ***REMOVED***
***REMOVED***

module.exports = AggregationCursor;
