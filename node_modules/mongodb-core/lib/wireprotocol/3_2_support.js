'use strict';

const Query = require('../connection/commands').Query;
const retrieveBSON = require('../connection/utils').retrieveBSON;
const MongoError = require('../error').MongoError;
const MongoNetworkError = require('../error').MongoNetworkError;
const getReadPreference = require('./shared').getReadPreference;
const BSON = retrieveBSON();
const Long = BSON.Long;
const ReadPreference = require('../topologies/read_preference');
const TxnState = require('../transactions').TxnState;

const WireProtocol = function() ***REMOVED******REMOVED***;

function isTransactionCommand(command) ***REMOVED***
  return !!(command.commitTransaction || command.abortTransaction);
***REMOVED***

/**
 * Optionally decorate a command with sessions specific keys
 *
 * @param ***REMOVED***Object***REMOVED*** command the command to decorate
 * @param ***REMOVED***ClientSession***REMOVED*** session the session tracking transaction state
 * @param ***REMOVED***Object***REMOVED*** [options] Optional settings passed to calling operation
 * @param ***REMOVED***Function***REMOVED*** [callback] Optional callback passed from calling operation
 * @return ***REMOVED***MongoError|null***REMOVED*** An error, if some error condition was met
 */
function decorateWithSessionsData(command, session, options) ***REMOVED***
  if (!session) ***REMOVED***
    return;
  ***REMOVED***

  // first apply non-transaction-specific sessions data
  const serverSession = session.serverSession;
  const inTransaction = session.inTransaction() || isTransactionCommand(command);
  const isRetryableWrite = options.willRetryWrite;

  if (serverSession.txnNumber && (isRetryableWrite || inTransaction)) ***REMOVED***
    command.txnNumber = BSON.Long.fromNumber(serverSession.txnNumber);
  ***REMOVED***

  // now attempt to apply transaction-specific sessions data
  if (!inTransaction) ***REMOVED***
    if (session.transaction.state !== TxnState.NO_TRANSACTION) ***REMOVED***
      session.transaction.transition(TxnState.NO_TRANSACTION);
    ***REMOVED***

    // for causal consistency
    if (session.supports.causalConsistency && session.operationTime) ***REMOVED***
      command.readConcern = command.readConcern || ***REMOVED******REMOVED***;
      Object.assign(command.readConcern, ***REMOVED*** afterClusterTime: session.operationTime ***REMOVED***);
    ***REMOVED***

    return;
  ***REMOVED***

  if (options.readPreference && !options.readPreference.equals(ReadPreference.primary)) ***REMOVED***
    return new MongoError(
      `Read preference in a transaction must be primary, not: $***REMOVED***options.readPreference.mode***REMOVED***`
    );
  ***REMOVED***

  // `autocommit` must always be false to differentiate from retryable writes
  command.autocommit = false;

  if (session.transaction.state === TxnState.STARTING_TRANSACTION) ***REMOVED***
    session.transaction.transition(TxnState.TRANSACTION_IN_PROGRESS);
    command.startTransaction = true;

    const readConcern =
      session.transaction.options.readConcern || session.clientOptions.readConcern;
    if (readConcern) ***REMOVED***
      command.readConcern = readConcern;
    ***REMOVED***

    if (session.supports.causalConsistency && session.operationTime) ***REMOVED***
      command.readConcern = command.readConcern || ***REMOVED******REMOVED***;
      Object.assign(command.readConcern, ***REMOVED*** afterClusterTime: session.operationTime ***REMOVED***);
    ***REMOVED***
  ***REMOVED***
***REMOVED***

//
// Execute a write operation
function executeWrite(pool, bson, type, opsField, ns, ops, options, callback) ***REMOVED***
  if (ops.length === 0) throw new MongoError('insert must contain at least one document');
  if (typeof options === 'function') ***REMOVED***
    callback = options;
    options = ***REMOVED******REMOVED***;
    options = options || ***REMOVED******REMOVED***;
  ***REMOVED***

  // Split the ns up to get db and collection
  const p = ns.split('.');
  const d = p.shift();
  // Options
  const ordered = typeof options.ordered === 'boolean' ? options.ordered : true;
  const writeConcern = options.writeConcern;

  // return skeleton
  const writeCommand = ***REMOVED******REMOVED***;
  writeCommand[type] = p.join('.');
  writeCommand[opsField] = ops;
  writeCommand.ordered = ordered;

  // Did we specify a write concern
  if (writeConcern && Object.keys(writeConcern).length > 0) ***REMOVED***
    writeCommand.writeConcern = writeConcern;
  ***REMOVED***

  // If we have collation passed in
  if (options.collation) ***REMOVED***
    for (let i = 0; i < writeCommand[opsField].length; i++) ***REMOVED***
      if (!writeCommand[opsField][i].collation) ***REMOVED***
        writeCommand[opsField][i].collation = options.collation;
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  // Do we have bypassDocumentValidation set, then enable it on the write command
  if (options.bypassDocumentValidation === true) ***REMOVED***
    writeCommand.bypassDocumentValidation = options.bypassDocumentValidation;
  ***REMOVED***

  // optionally decorate command with transactions data
  const err = decorateWithSessionsData(writeCommand, options.session, options, callback);
  if (err) ***REMOVED***
    return callback(err, null);
  ***REMOVED***

  // Options object
  const opts = ***REMOVED*** command: true ***REMOVED***;
  if (typeof options.session !== 'undefined') opts.session = options.session;
  const queryOptions = ***REMOVED*** checkKeys: false, numberToSkip: 0, numberToReturn: 1 ***REMOVED***;
  if (type === 'insert') queryOptions.checkKeys = false;
  if (typeof options.checkKeys === 'boolean') queryOptions.checkKeys = options.checkKeys;

  // Ensure we support serialization of functions
  if (options.serializeFunctions) queryOptions.serializeFunctions = options.serializeFunctions;
  // Do not serialize the undefined fields
  if (options.ignoreUndefined) queryOptions.ignoreUndefined = options.ignoreUndefined;

  try ***REMOVED***
    // Create write command
    const cmd = new Query(bson, `$***REMOVED***d***REMOVED***.$cmd`, writeCommand, queryOptions);
    // Execute command
    pool.write(cmd, opts, callback);
  ***REMOVED*** catch (err) ***REMOVED***
    callback(err);
  ***REMOVED***
***REMOVED***

//
// Needs to support legacy mass insert as well as ordered/unordered legacy
// emulation
//
WireProtocol.prototype.insert = function(pool, ns, bson, ops, options, callback) ***REMOVED***
  executeWrite(pool, bson, 'insert', 'documents', ns, ops, options, callback);
***REMOVED***;

WireProtocol.prototype.update = function(pool, ns, bson, ops, options, callback) ***REMOVED***
  executeWrite(pool, bson, 'update', 'updates', ns, ops, options, callback);
***REMOVED***;

WireProtocol.prototype.remove = function(pool, ns, bson, ops, options, callback) ***REMOVED***
  executeWrite(pool, bson, 'delete', 'deletes', ns, ops, options, callback);
***REMOVED***;

WireProtocol.prototype.killCursor = function(bson, ns, cursorState, pool, callback) ***REMOVED***
  // Build command namespace
  const parts = ns.split(/\./);
  // Command namespace
  const commandns = `$***REMOVED***parts.shift()***REMOVED***.$cmd`;
  const cursorId = cursorState.cursorId;
  // Create killCursor command
  const killcursorCmd = ***REMOVED***
    killCursors: parts.join('.'),
    cursors: [cursorId]
  ***REMOVED***;

  // Build Query object
  const query = new Query(bson, commandns, killcursorCmd, ***REMOVED***
    numberToSkip: 0,
    numberToReturn: -1,
    checkKeys: false,
    returnFieldSelector: null
  ***REMOVED***);

  // Kill cursor callback
  function killCursorCallback(err, result) ***REMOVED***
    if (err) ***REMOVED***
      if (typeof callback !== 'function') return;
      return callback(err);
    ***REMOVED***

    // Result
    const r = result.message;
    // If we have a timed out query or a cursor that was killed
    if ((r.responseFlags & (1 << 0)) !== 0) ***REMOVED***
      if (typeof callback !== 'function') return;
      return callback(new MongoNetworkError('cursor killed or timed out'), null);
    ***REMOVED***

    if (!Array.isArray(r.documents) || r.documents.length === 0) ***REMOVED***
      if (typeof callback !== 'function') return;
      return callback(
        new MongoError(`invalid killCursors result returned for cursor id $***REMOVED***cursorId***REMOVED***`)
      );
    ***REMOVED***

    // Return the result
    if (typeof callback === 'function') ***REMOVED***
      callback(null, r.documents[0]);
    ***REMOVED***
  ***REMOVED***

  const options = ***REMOVED*** command: true ***REMOVED***;
  if (typeof cursorState.session === 'object') ***REMOVED***
    options.session = cursorState.session;
  ***REMOVED***

  // Execute the kill cursor command
  if (pool && pool.isConnected()) ***REMOVED***
    try ***REMOVED***
      pool.write(query, options, killCursorCallback);
    ***REMOVED*** catch (err) ***REMOVED***
      killCursorCallback(err, null);
    ***REMOVED***

    return;
  ***REMOVED***

  // Callback
  if (typeof callback === 'function') callback(null, null);
***REMOVED***;

WireProtocol.prototype.getMore = function(
  bson,
  ns,
  cursorState,
  batchSize,
  raw,
  connection,
  options,
  callback
) ***REMOVED***
  options = options || ***REMOVED******REMOVED***;
  // Build command namespace
  const parts = ns.split(/\./);
  // Command namespace
  const commandns = `$***REMOVED***parts.shift()***REMOVED***.$cmd`;

  // Create getMore command
  const getMoreCmd = ***REMOVED***
    getMore: cursorState.cursorId,
    collection: parts.join('.'),
    batchSize: Math.abs(batchSize)
  ***REMOVED***;

  // optionally decorate command with transactions data
  const err = decorateWithSessionsData(getMoreCmd, options.session, options, callback);
  if (err) ***REMOVED***
    return callback(err, null);
  ***REMOVED***

  if (cursorState.cmd.tailable && typeof cursorState.cmd.maxAwaitTimeMS === 'number') ***REMOVED***
    getMoreCmd.maxTimeMS = cursorState.cmd.maxAwaitTimeMS;
  ***REMOVED***

  // Build Query object
  const query = new Query(bson, commandns, getMoreCmd, ***REMOVED***
    numberToSkip: 0,
    numberToReturn: -1,
    checkKeys: false,
    returnFieldSelector: null
  ***REMOVED***);

  // Query callback
  function queryCallback(err, result) ***REMOVED***
    if (err) return callback(err);
    // Get the raw message
    const r = result.message;

    // If we have a timed out query or a cursor that was killed
    if ((r.responseFlags & (1 << 0)) !== 0) ***REMOVED***
      return callback(new MongoNetworkError('cursor killed or timed out'), null);
    ***REMOVED***

    // Raw, return all the extracted documents
    if (raw) ***REMOVED***
      cursorState.documents = r.documents;
      cursorState.cursorId = r.cursorId;
      return callback(null, r.documents);
    ***REMOVED***

    // We have an error detected
    if (r.documents[0].ok === 0) ***REMOVED***
      return callback(new MongoError(r.documents[0]));
    ***REMOVED***

    // Ensure we have a Long valid cursor id
    const cursorId =
      typeof r.documents[0].cursor.id === 'number'
        ? Long.fromNumber(r.documents[0].cursor.id)
        : r.documents[0].cursor.id;

    // Set all the values
    cursorState.documents = r.documents[0].cursor.nextBatch;
    cursorState.cursorId = cursorId;

    // Return the result
    callback(null, r.documents[0], r.connection);
  ***REMOVED***

  // Query options
  const queryOptions = ***REMOVED*** command: true ***REMOVED***;

  // If we have a raw query decorate the function
  if (raw) ***REMOVED***
    queryOptions.raw = raw;
  ***REMOVED***

  // Add the result field needed
  queryOptions.documentsReturnedIn = 'nextBatch';

  // Check if we need to promote longs
  if (typeof cursorState.promoteLongs === 'boolean') ***REMOVED***
    queryOptions.promoteLongs = cursorState.promoteLongs;
  ***REMOVED***

  if (typeof cursorState.promoteValues === 'boolean') ***REMOVED***
    queryOptions.promoteValues = cursorState.promoteValues;
  ***REMOVED***

  if (typeof cursorState.promoteBuffers === 'boolean') ***REMOVED***
    queryOptions.promoteBuffers = cursorState.promoteBuffers;
  ***REMOVED***

  if (typeof cursorState.session === 'object') ***REMOVED***
    queryOptions.session = cursorState.session;
  ***REMOVED***

  // Write out the getMore command
  connection.write(query, queryOptions, queryCallback);
***REMOVED***;

WireProtocol.prototype.command = function(bson, ns, cmd, cursorState, topology, options) ***REMOVED***
  options = options || ***REMOVED******REMOVED***;
  // Check if this is a wire protocol command or not
  const wireProtocolCommand =
    typeof options.wireProtocolCommand === 'boolean' ? options.wireProtocolCommand : true;

  // Establish type of command
  let query;
  if (cmd.find && wireProtocolCommand) ***REMOVED***
    // Create the find command
    query = executeFindCommand(bson, ns, cmd, cursorState, topology, options);

    // Mark the cmd as virtual
    cmd.virtual = false;
    // Signal the documents are in the firstBatch value
    query.documentsReturnedIn = 'firstBatch';
  ***REMOVED*** else if (cursorState.cursorId != null) ***REMOVED***
    return;
  ***REMOVED*** else if (cmd) ***REMOVED***
    query = setupCommand(bson, ns, cmd, cursorState, topology, options);
  ***REMOVED*** else ***REMOVED***
    return new MongoError(`command $***REMOVED***JSON.stringify(cmd)***REMOVED*** does not return a cursor`);
  ***REMOVED***

  if (query instanceof MongoError) ***REMOVED***
    return query;
  ***REMOVED***

  // optionally decorate query with transaction data
  const err = decorateWithSessionsData(query.query, options.session, options);
  if (err) ***REMOVED***
    return err;
  ***REMOVED***

  return query;
***REMOVED***;

// // Command
// ***REMOVED***
//     find: ns
//   , query: <object>
//   , limit: <n>
//   , fields: <object>
//   , skip: <n>
//   , hint: <string>
//   , explain: <boolean>
//   , snapshot: <boolean>
//   , batchSize: <n>
//   , returnKey: <boolean>
//   , maxScan: <n>
//   , min: <n>
//   , max: <n>
//   , showDiskLoc: <boolean>
//   , comment: <string>
//   , maxTimeMS: <n>
//   , raw: <boolean>
//   , readPreference: <ReadPreference>
//   , tailable: <boolean>
//   , oplogReplay: <boolean>
//   , noCursorTimeout: <boolean>
//   , awaitdata: <boolean>
//   , exhaust: <boolean>
//   , partial: <boolean>
// ***REMOVED***

// FIND/GETMORE SPEC
// ***REMOVED***
//     “find”: <string>,
//     “filter”: ***REMOVED*** ... ***REMOVED***,
//     “sort”: ***REMOVED*** ... ***REMOVED***,
//     “projection”: ***REMOVED*** ... ***REMOVED***,
//     “hint”: ***REMOVED*** ... ***REMOVED***,
//     “skip”: <int>,
//     “limit”: <int>,
//     “batchSize”: <int>,
//     “singleBatch”: <bool>,
//     “comment”: <string>,
//     “maxScan”: <int>,
//     “maxTimeMS”: <int>,
//     “max”: ***REMOVED*** ... ***REMOVED***,
//     “min”: ***REMOVED*** ... ***REMOVED***,
//     “returnKey”: <bool>,
//     “showRecordId”: <bool>,
//     “snapshot”: <bool>,
//     “tailable”: <bool>,
//     “oplogReplay”: <bool>,
//     “noCursorTimeout”: <bool>,
//     “awaitData”: <bool>,
//     “partial”: <bool>,
//     “$readPreference”: ***REMOVED*** ... ***REMOVED***
// ***REMOVED***

//
// Execute a find command
function executeFindCommand(bson, ns, cmd, cursorState, topology, options) ***REMOVED***
  // Ensure we have at least some options
  options = options || ***REMOVED******REMOVED***;
  // Get the readPreference
  const readPreference = getReadPreference(cmd, options);

  // Set the optional batchSize
  cursorState.batchSize = cmd.batchSize || cursorState.batchSize;

  // Build command namespace
  const parts = ns.split(/\./);
  // Command namespace
  const commandns = `$***REMOVED***parts.shift()***REMOVED***.$cmd`;

  // Build actual find command
  let findCmd = ***REMOVED***
    find: parts.join('.')
  ***REMOVED***;

  // I we provided a filter
  if (cmd.query) ***REMOVED***
    // Check if the user is passing in the $query parameter
    if (cmd.query['$query']) ***REMOVED***
      findCmd.filter = cmd.query['$query'];
    ***REMOVED*** else ***REMOVED***
      findCmd.filter = cmd.query;
    ***REMOVED***
  ***REMOVED***

  // Sort value
  let sortValue = cmd.sort;

  // Handle issue of sort being an Array
  if (Array.isArray(sortValue)) ***REMOVED***
    const sortObject = ***REMOVED******REMOVED***;

    if (sortValue.length > 0 && !Array.isArray(sortValue[0])) ***REMOVED***
      let sortDirection = sortValue[1];
      // Translate the sort order text
      if (sortDirection === 'asc') ***REMOVED***
        sortDirection = 1;
      ***REMOVED*** else if (sortDirection === 'desc') ***REMOVED***
        sortDirection = -1;
      ***REMOVED***

      // Set the sort order
      sortObject[sortValue[0]] = sortDirection;
    ***REMOVED*** else ***REMOVED***
      for (var i = 0; i < sortValue.length; i++) ***REMOVED***
        let sortDirection = sortValue[i][1];
        // Translate the sort order text
        if (sortDirection === 'asc') ***REMOVED***
          sortDirection = 1;
        ***REMOVED*** else if (sortDirection === 'desc') ***REMOVED***
          sortDirection = -1;
        ***REMOVED***

        // Set the sort order
        sortObject[sortValue[i][0]] = sortDirection;
      ***REMOVED***
    ***REMOVED***

    sortValue = sortObject;
  ***REMOVED***

  // Add sort to command
  if (cmd.sort) findCmd.sort = sortValue;
  // Add a projection to the command
  if (cmd.fields) findCmd.projection = cmd.fields;
  // Add a hint to the command
  if (cmd.hint) findCmd.hint = cmd.hint;
  // Add a skip
  if (cmd.skip) findCmd.skip = cmd.skip;
  // Add a limit
  if (cmd.limit) findCmd.limit = cmd.limit;

  // Check if we wish to have a singleBatch
  if (cmd.limit < 0) ***REMOVED***
    findCmd.limit = Math.abs(cmd.limit);
    findCmd.singleBatch = true;
  ***REMOVED***

  // Add a batchSize
  if (typeof cmd.batchSize === 'number') ***REMOVED***
    if (cmd.batchSize < 0) ***REMOVED***
      if (cmd.limit !== 0 && Math.abs(cmd.batchSize) < Math.abs(cmd.limit)) ***REMOVED***
        findCmd.limit = Math.abs(cmd.batchSize);
      ***REMOVED***

      findCmd.singleBatch = true;
    ***REMOVED***

    findCmd.batchSize = Math.abs(cmd.batchSize);
  ***REMOVED***

  // If we have comment set
  if (cmd.comment) findCmd.comment = cmd.comment;

  // If we have maxScan
  if (cmd.maxScan) findCmd.maxScan = cmd.maxScan;

  // If we have maxTimeMS set
  if (cmd.maxTimeMS) findCmd.maxTimeMS = cmd.maxTimeMS;

  // If we have min
  if (cmd.min) findCmd.min = cmd.min;

  // If we have max
  if (cmd.max) findCmd.max = cmd.max;

  // If we have returnKey set
  findCmd.returnKey = cmd.returnKey ? cmd.returnKey : false;

  // If we have showDiskLoc set
  findCmd.showRecordId = cmd.showDiskLoc ? cmd.showDiskLoc : false;

  // If we have snapshot set
  if (cmd.snapshot) findCmd.snapshot = cmd.snapshot;

  // If we have tailable set
  if (cmd.tailable) findCmd.tailable = cmd.tailable;

  // If we have oplogReplay set
  if (cmd.oplogReplay) findCmd.oplogReplay = cmd.oplogReplay;

  // If we have noCursorTimeout set
  if (cmd.noCursorTimeout) findCmd.noCursorTimeout = cmd.noCursorTimeout;

  // If we have awaitData set
  if (cmd.awaitData) findCmd.awaitData = cmd.awaitData;
  if (cmd.awaitdata) findCmd.awaitData = cmd.awaitdata;

  // If we have partial set
  if (cmd.partial) findCmd.partial = cmd.partial;

  // If we have collation passed in
  if (cmd.collation) findCmd.collation = cmd.collation;

  // If we have explain, we need to rewrite the find command
  // to wrap it in the explain command
  if (cmd.explain) ***REMOVED***
    findCmd = ***REMOVED***
      explain: findCmd
    ***REMOVED***;
  ***REMOVED***

  // Did we provide a readConcern
  if (cmd.readConcern) findCmd.readConcern = cmd.readConcern;

  // Set up the serialize and ignoreUndefined fields
  const serializeFunctions =
    typeof options.serializeFunctions === 'boolean' ? options.serializeFunctions : false;
  const ignoreUndefined =
    typeof options.ignoreUndefined === 'boolean' ? options.ignoreUndefined : false;

  // We have a Mongos topology, check if we need to add a readPreference
  if (topology.type === 'mongos' && readPreference && readPreference.preference !== 'primary') ***REMOVED***
    findCmd = ***REMOVED***
      $query: findCmd,
      $readPreference: readPreference.toJSON()
    ***REMOVED***;
  ***REMOVED***

  // optionally decorate query with transaction data
  const err = decorateWithSessionsData(findCmd, options.session, options);
  if (err) ***REMOVED***
    return err;
  ***REMOVED***

  // Build Query object
  const query = new Query(bson, commandns, findCmd, ***REMOVED***
    numberToSkip: 0,
    numberToReturn: 1,
    checkKeys: false,
    returnFieldSelector: null,
    serializeFunctions: serializeFunctions,
    ignoreUndefined: ignoreUndefined
  ***REMOVED***);

  // Set query flags
  query.slaveOk = readPreference.slaveOk();

  // Return the query
  return query;
***REMOVED***

//
// Set up a command cursor
function setupCommand(bson, ns, cmd, cursorState, topology, options) ***REMOVED***
  // Set empty options object
  options = options || ***REMOVED******REMOVED***;
  // Get the readPreference
  const readPreference = getReadPreference(cmd, options);

  // Final query
  let finalCmd = ***REMOVED******REMOVED***;
  for (let name in cmd) ***REMOVED***
    finalCmd[name] = cmd[name];
  ***REMOVED***

  // Build command namespace
  const parts = ns.split(/\./);

  // Serialize functions
  const serializeFunctions =
    typeof options.serializeFunctions === 'boolean' ? options.serializeFunctions : false;

  // Set up the serialize and ignoreUndefined fields
  const ignoreUndefined =
    typeof options.ignoreUndefined === 'boolean' ? options.ignoreUndefined : false;

  // We have a Mongos topology, check if we need to add a readPreference
  if (topology.type === 'mongos' && readPreference && readPreference.preference !== 'primary') ***REMOVED***
    finalCmd = ***REMOVED***
      $query: finalCmd,
      $readPreference: readPreference.toJSON()
    ***REMOVED***;
  ***REMOVED***

  // optionally decorate query with transaction data
  const err = decorateWithSessionsData(finalCmd, options.session, options);
  if (err) ***REMOVED***
    return err;
  ***REMOVED***

  // Build Query object
  const query = new Query(bson, `$***REMOVED***parts.shift()***REMOVED***.$cmd`, finalCmd, ***REMOVED***
    numberToSkip: 0,
    numberToReturn: -1,
    checkKeys: false,
    serializeFunctions: serializeFunctions,
    ignoreUndefined: ignoreUndefined
  ***REMOVED***);

  // Set query flags
  query.slaveOk = readPreference.slaveOk();

  // Return the query
  return query;
***REMOVED***

module.exports = WireProtocol;
