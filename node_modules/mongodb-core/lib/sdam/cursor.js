'use strict';

const Logger = require('../connection/logger');
const BSON = require('../connection/utils').retrieveBSON();
const MongoError = require('../error').MongoError;
const MongoNetworkError = require('../error').MongoNetworkError;
const mongoErrorContextSymbol = require('../error').mongoErrorContextSymbol;
const Long = BSON.Long;
const deprecate = require('util').deprecate;
const readPreferenceServerSelector = require('./server_selectors').readPreferenceServerSelector;
const ReadPreference = require('../topologies/read_preference');

/**
 * Handle callback (including any exceptions thrown)
 */
function handleCallback(callback, err, result) ***REMOVED***
  try ***REMOVED***
    callback(err, result);
  ***REMOVED*** catch (err) ***REMOVED***
    process.nextTick(function() ***REMOVED***
      throw err;
    ***REMOVED***);
  ***REMOVED***
***REMOVED***

/**
 * This is a cursor results callback
 *
 * @callback resultCallback
 * @param ***REMOVED***error***REMOVED*** error An error object. Set to null if no error present
 * @param ***REMOVED***object***REMOVED*** document
 */

/**
 * An internal class that embodies a cursor on MongoDB, allowing for iteration over the
 * results returned from a query.
 *
 * @property ***REMOVED***number***REMOVED*** cursorBatchSize The current cursorBatchSize for the cursor
 * @property ***REMOVED***number***REMOVED*** cursorLimit The current cursorLimit for the cursor
 * @property ***REMOVED***number***REMOVED*** cursorSkip The current cursorSkip for the cursor
 */
class Cursor ***REMOVED***
  /**
   * Create a cursor
   *
   * @param ***REMOVED***object***REMOVED*** bson An instance of the BSON parser
   * @param ***REMOVED***string***REMOVED*** ns The MongoDB fully qualified namespace (ex: db1.collection1)
   * @param ***REMOVED******REMOVED***object***REMOVED***|Long***REMOVED*** cmd The selector (can be a command or a cursorId)
   * @param ***REMOVED***object***REMOVED*** [options=null] Optional settings.
   * @param ***REMOVED***object***REMOVED*** [options.batchSize=1000] Batchsize for the operation
   * @param ***REMOVED***array***REMOVED*** [options.documents=[]] Initial documents list for cursor
   * @param ***REMOVED***object***REMOVED*** [options.transforms=null] Transform methods for the cursor results
   * @param ***REMOVED***function***REMOVED*** [options.transforms.query] Transform the value returned from the initial query
   * @param ***REMOVED***function***REMOVED*** [options.transforms.doc] Transform each document returned from Cursor.prototype.next
   * @param ***REMOVED***object***REMOVED*** topology The server topology instance.
   * @param ***REMOVED***object***REMOVED*** topologyOptions The server topology options.
   */
  constructor(bson, ns, cmd, options, topology, topologyOptions) ***REMOVED***
    options = options || ***REMOVED******REMOVED***;

    // Cursor pool
    this.pool = null;
    // Cursor server
    this.server = null;

    // Do we have a not connected handler
    this.disconnectHandler = options.disconnectHandler;

    // Set local values
    this.bson = bson;
    this.ns = ns;
    this.cmd = cmd;
    this.options = options;
    this.topology = topology;

    // All internal state
    this.s = ***REMOVED***
      cursorId: null,
      cmd: cmd,
      documents: options.documents || [],
      cursorIndex: 0,
      dead: false,
      killed: false,
      init: false,
      notified: false,
      limit: options.limit || cmd.limit || 0,
      skip: options.skip || cmd.skip || 0,
      batchSize: options.batchSize || cmd.batchSize || 1000,
      currentLimit: 0,
      // Result field name if not a cursor (contains the array of results)
      transforms: options.transforms
    ***REMOVED***;

    if (typeof options.session === 'object') ***REMOVED***
      this.s.session = options.session;
    ***REMOVED***

    // Add promoteLong to cursor state
    if (typeof topologyOptions.promoteLongs === 'boolean') ***REMOVED***
      this.s.promoteLongs = topologyOptions.promoteLongs;
    ***REMOVED*** else if (typeof options.promoteLongs === 'boolean') ***REMOVED***
      this.s.promoteLongs = options.promoteLongs;
    ***REMOVED***

    // Add promoteValues to cursor state
    if (typeof topologyOptions.promoteValues === 'boolean') ***REMOVED***
      this.s.promoteValues = topologyOptions.promoteValues;
    ***REMOVED*** else if (typeof options.promoteValues === 'boolean') ***REMOVED***
      this.s.promoteValues = options.promoteValues;
    ***REMOVED***

    // Add promoteBuffers to cursor state
    if (typeof topologyOptions.promoteBuffers === 'boolean') ***REMOVED***
      this.s.promoteBuffers = topologyOptions.promoteBuffers;
    ***REMOVED*** else if (typeof options.promoteBuffers === 'boolean') ***REMOVED***
      this.s.promoteBuffers = options.promoteBuffers;
    ***REMOVED***

    if (topologyOptions.reconnect) ***REMOVED***
      this.s.reconnect = topologyOptions.reconnect;
    ***REMOVED***

    // Logger
    this.logger = Logger('Cursor', topologyOptions);

    //
    // Did we pass in a cursor id
    if (typeof cmd === 'number') ***REMOVED***
      this.s.cursorId = Long.fromNumber(cmd);
      this.s.lastCursorId = this.s.cursorId;
    ***REMOVED*** else if (cmd instanceof Long) ***REMOVED***
      this.s.cursorId = cmd;
      this.s.lastCursorId = cmd;
    ***REMOVED***
  ***REMOVED***

  setCursorBatchSize(value) ***REMOVED***
    this.s.batchSize = value;
  ***REMOVED***

  cursorBatchSize() ***REMOVED***
    return this.s.batchSize;
  ***REMOVED***

  setCursorLimit(value) ***REMOVED***
    this.s.limit = value;
  ***REMOVED***

  cursorLimit() ***REMOVED***
    return this.s.limit;
  ***REMOVED***

  setCursorSkip(value) ***REMOVED***
    this.s.skip = value;
  ***REMOVED***

  cursorSkip() ***REMOVED***
    return this.s.skip;
  ***REMOVED***

  _endSession(options, callback) ***REMOVED***
    if (typeof options === 'function') ***REMOVED***
      callback = options;
      options = ***REMOVED******REMOVED***;
    ***REMOVED***
    options = options || ***REMOVED******REMOVED***;

    const session = this.s.session;
    if (session && (options.force || session.owner === this)) ***REMOVED***
      this.s.session = undefined;
      session.endSession(callback);
      return true;
    ***REMOVED***

    if (callback) ***REMOVED***
      callback();
    ***REMOVED***

    return false;
  ***REMOVED***

  /**
   * Clone the cursor
   * @method
   * @return ***REMOVED***Cursor***REMOVED***
   */
  clone() ***REMOVED***
    return this.topology.cursor(this.ns, this.cmd, this.options);
  ***REMOVED***

  /**
   * Checks if the cursor is dead
   * @method
   * @return ***REMOVED***boolean***REMOVED*** A boolean signifying if the cursor is dead or not
   */
  isDead() ***REMOVED***
    return this.s.dead === true;
  ***REMOVED***

  /**
   * Checks if the cursor was killed by the application
   * @method
   * @return ***REMOVED***boolean***REMOVED*** A boolean signifying if the cursor was killed by the application
   */
  isKilled() ***REMOVED***
    return this.s.killed === true;
  ***REMOVED***

  /**
   * Checks if the cursor notified it's caller about it's death
   * @method
   * @return ***REMOVED***boolean***REMOVED*** A boolean signifying if the cursor notified the callback
   */
  isNotified() ***REMOVED***
    return this.s.notified === true;
  ***REMOVED***

  /**
   * Returns current buffered documents length
   * @method
   * @return ***REMOVED***number***REMOVED*** The number of items in the buffered documents
   */
  bufferedCount() ***REMOVED***
    return this.s.documents.length - this.s.cursorIndex;
  ***REMOVED***

  /**
   * Kill the cursor
   *
   * @param ***REMOVED***resultCallback***REMOVED*** callback A callback function
   */
  kill(callback) ***REMOVED***
    // Set cursor to dead
    this.s.dead = true;
    this.s.killed = true;
    // Remove documents
    this.s.documents = [];

    // If no cursor id just return
    if (this.s.cursorId == null || this.s.cursorId.isZero() || this.s.init === false) ***REMOVED***
      if (callback) callback(null, null);
      return;
    ***REMOVED***

    // Default pool
    const pool = this.s.server.s.pool;

    // Execute command
    this.s.server.s.wireProtocolHandler.killCursor(this.bson, this.ns, this.s, pool, callback);
  ***REMOVED***

  /**
   * Resets the cursor
   */
  rewind() ***REMOVED***
    if (this.s.init) ***REMOVED***
      if (!this.s.dead) ***REMOVED***
        this.kill();
      ***REMOVED***

      this.s.currentLimit = 0;
      this.s.init = false;
      this.s.dead = false;
      this.s.killed = false;
      this.s.notified = false;
      this.s.documents = [];
      this.s.cursorId = null;
      this.s.cursorIndex = 0;
    ***REMOVED***
  ***REMOVED***

  /**
   * Returns current buffered documents
   * @method
   * @return ***REMOVED***Array***REMOVED*** An array of buffered documents
   */
  readBufferedDocuments(number) ***REMOVED***
    const unreadDocumentsLength = this.s.documents.length - this.s.cursorIndex;
    const length = number < unreadDocumentsLength ? number : unreadDocumentsLength;
    let elements = this.s.documents.slice(this.s.cursorIndex, this.s.cursorIndex + length);

    // Transform the doc with passed in transformation method if provided
    if (this.s.transforms && typeof this.s.transforms.doc === 'function') ***REMOVED***
      // Transform all the elements
      for (let i = 0; i < elements.length; i++) ***REMOVED***
        elements[i] = this.s.transforms.doc(elements[i]);
      ***REMOVED***
    ***REMOVED***

    // Ensure we do not return any more documents than the limit imposed
    // Just return the number of elements up to the limit
    if (this.s.limit > 0 && this.s.currentLimit + elements.length > this.s.limit) ***REMOVED***
      elements = elements.slice(0, this.s.limit - this.s.currentLimit);
      this.kill();
    ***REMOVED***

    // Adjust current limit
    this.s.currentLimit = this.s.currentLimit + elements.length;
    this.s.cursorIndex = this.s.cursorIndex + elements.length;

    // Return elements
    return elements;
  ***REMOVED***

  /**
   * Retrieve the next document from the cursor
   *
   * @param ***REMOVED***resultCallback***REMOVED*** callback A callback function
   */
  next(callback) ***REMOVED***
    nextFunction(this, callback);
  ***REMOVED***
***REMOVED***

Cursor.prototype._find = deprecate(
  callback => _find(this, callback),
  '_find() is deprecated, please stop using it'
);

Cursor.prototype._getmore = deprecate(
  callback => _getmore(this, callback),
  '_getmore() is deprecated, please stop using it'
);

function _getmore(cursor, callback) ***REMOVED***
  if (cursor.logger.isDebug()) ***REMOVED***
    cursor.logger.debug(`schedule getMore call for query [$***REMOVED***JSON.stringify(cursor.query)***REMOVED***]`);
  ***REMOVED***

  // Determine if it's a raw query
  const raw = cursor.options.raw || cursor.cmd.raw;

  // Set the current batchSize
  let batchSize = cursor.s.batchSize;
  if (cursor.s.limit > 0 && cursor.s.currentLimit + batchSize > cursor.s.limit) ***REMOVED***
    batchSize = cursor.s.limit - cursor.s.currentLimit;
  ***REMOVED***

  // Default pool
  const pool = cursor.s.server.s.pool;

  // We have a wire protocol handler
  cursor.s.server.s.wireProtocolHandler.getMore(
    cursor.bson,
    cursor.ns,
    cursor.s,
    batchSize,
    raw,
    pool,
    cursor.options,
    callback
  );
***REMOVED***

function _find(cursor, callback) ***REMOVED***
  if (cursor.logger.isDebug()) ***REMOVED***
    cursor.logger.debug(
      `issue initial query [$***REMOVED***JSON.stringify(cursor.cmd)***REMOVED***] with flags [$***REMOVED***JSON.stringify(
        cursor.query
      )***REMOVED***]`
    );
  ***REMOVED***

  const queryCallback = (err, r) => ***REMOVED***
    if (err) return callback(err);

    // Get the raw message
    const result = r.message;

    // Query failure bit set
    if (result.queryFailure) ***REMOVED***
      return callback(new MongoError(result.documents[0]), null);
    ***REMOVED***

    // Check if we have a command cursor
    if (
      Array.isArray(result.documents) &&
      result.documents.length === 1 &&
      (!cursor.cmd.find || (cursor.cmd.find && cursor.cmd.virtual === false)) &&
      (result.documents[0].cursor !== 'string' ||
        result.documents[0]['$err'] ||
        result.documents[0]['errmsg'] ||
        Array.isArray(result.documents[0].result))
    ) ***REMOVED***
      // We have a an error document return the error
      if (result.documents[0]['$err'] || result.documents[0]['errmsg']) ***REMOVED***
        return callback(new MongoError(result.documents[0]), null);
      ***REMOVED***

      // We have a cursor document
      if (result.documents[0].cursor != null && typeof result.documents[0].cursor !== 'string') ***REMOVED***
        const id = result.documents[0].cursor.id;
        // If we have a namespace change set the new namespace for getmores
        if (result.documents[0].cursor.ns) ***REMOVED***
          cursor.ns = result.documents[0].cursor.ns;
        ***REMOVED***
        // Promote id to long if needed
        cursor.s.cursorId = typeof id === 'number' ? Long.fromNumber(id) : id;
        cursor.s.lastCursorId = cursor.s.cursorId;
        // If we have a firstBatch set it
        if (Array.isArray(result.documents[0].cursor.firstBatch)) ***REMOVED***
          cursor.s.documents = result.documents[0].cursor.firstBatch;
        ***REMOVED***

        // Return after processing command cursor
        return callback(null, result);
      ***REMOVED***

      if (Array.isArray(result.documents[0].result)) ***REMOVED***
        cursor.s.documents = result.documents[0].result;
        cursor.s.cursorId = Long.ZERO;
        return callback(null, result);
      ***REMOVED***
    ***REMOVED***

    // Otherwise fall back to regular find path
    cursor.s.cursorId = result.cursorId;
    cursor.s.documents = result.documents;
    cursor.s.lastCursorId = result.cursorId;

    // Transform the results with passed in transformation method if provided
    if (cursor.s.transforms && typeof cursor.s.transforms.query === 'function') ***REMOVED***
      cursor.s.documents = cursor.s.transforms.query(result);
    ***REMOVED***

    // Return callback
    callback(null, result);
  ***REMOVED***;

  // Options passed to the pool
  const queryOptions = ***REMOVED******REMOVED***;

  // If we have a raw query decorate the function
  if (cursor.options.raw || cursor.cmd.raw) ***REMOVED***
    queryOptions.raw = cursor.options.raw || cursor.cmd.raw;
  ***REMOVED***

  // Do we have documentsReturnedIn set on the query
  if (typeof cursor.query.documentsReturnedIn === 'string') ***REMOVED***
    queryOptions.documentsReturnedIn = cursor.query.documentsReturnedIn;
  ***REMOVED***

  // Add promote Long value if defined
  if (typeof cursor.s.promoteLongs === 'boolean') ***REMOVED***
    queryOptions.promoteLongs = cursor.s.promoteLongs;
  ***REMOVED***

  // Add promote values if defined
  if (typeof cursor.s.promoteValues === 'boolean') ***REMOVED***
    queryOptions.promoteValues = cursor.s.promoteValues;
  ***REMOVED***

  // Add promote values if defined
  if (typeof cursor.s.promoteBuffers === 'boolean') ***REMOVED***
    queryOptions.promoteBuffers = cursor.s.promoteBuffers;
  ***REMOVED***

  if (typeof cursor.s.session === 'object') ***REMOVED***
    queryOptions.session = cursor.s.session;
  ***REMOVED***

  // Write the initial command out
  cursor.s.server.s.pool.write(cursor.query, queryOptions, queryCallback);
***REMOVED***

/**
 * Validate if the pool is dead and return error
 */
function isConnectionDead(cursor, callback) ***REMOVED***
  if (cursor.pool && cursor.pool.isDestroyed()) ***REMOVED***
    cursor.s.killed = true;
    const err = new MongoNetworkError(
      `connection to host $***REMOVED***cursor.pool.host***REMOVED***:$***REMOVED***cursor.pool.port***REMOVED*** was destroyed`
    );
    _setCursorNotifiedImpl(cursor, () => callback(err));
    return true;
  ***REMOVED***

  return false;
***REMOVED***

/**
 * Validate if the cursor is dead but was not explicitly killed by user
 */
function isCursorDeadButNotkilled(cursor, callback) ***REMOVED***
  // Cursor is dead but not marked killed, return null
  if (cursor.s.dead && !cursor.s.killed) ***REMOVED***
    cursor.s.killed = true;
    setCursorNotified(cursor, callback);
    return true;
  ***REMOVED***

  return false;
***REMOVED***

/**
 * Validate if the cursor is dead and was killed by user
 */
function isCursorDeadAndKilled(cursor, callback) ***REMOVED***
  if (cursor.s.dead && cursor.s.killed) ***REMOVED***
    handleCallback(callback, new MongoError('cursor is dead'));
    return true;
  ***REMOVED***

  return false;
***REMOVED***

/**
 * Validate if the cursor was killed by the user
 */
function isCursorKilled(cursor, callback) ***REMOVED***
  if (cursor.s.killed) ***REMOVED***
    setCursorNotified(cursor, callback);
    return true;
  ***REMOVED***

  return false;
***REMOVED***

/**
 * Mark cursor as being dead and notified
 */
function setCursorDeadAndNotified(cursor, callback) ***REMOVED***
  cursor.s.dead = true;
  setCursorNotified(cursor, callback);
***REMOVED***

/**
 * Mark cursor as being notified
 */
function setCursorNotified(cursor, callback) ***REMOVED***
  _setCursorNotifiedImpl(cursor, () => handleCallback(callback, null, null));
***REMOVED***

function _setCursorNotifiedImpl(cursor, callback) ***REMOVED***
  cursor.s.notified = true;
  cursor.s.documents = [];
  cursor.s.cursorIndex = 0;
  if (cursor._endSession) ***REMOVED***
    return cursor._endSession(undefined, () => callback());
  ***REMOVED***
  return callback();
***REMOVED***

function initializeCursorAndRetryNext(cursor, callback) ***REMOVED***
  cursor.topology.selectServer(
    readPreferenceServerSelector(cursor.options.readPreference || ReadPreference.primary),
    (err, server) => ***REMOVED***
      if (err) ***REMOVED***
        callback(err, null);
        return;
      ***REMOVED***

      cursor.s.server = server;
      cursor.s.init = true;

      // check if server supports collation
      // NOTE: this should be a part of the selection predicate!
      if (cursor.cmd && cursor.cmd.collation && cursor.server.description.maxWireVersion < 5) ***REMOVED***
        callback(new MongoError(`server $***REMOVED***cursor.server.name***REMOVED*** does not support collation`));
        return;
      ***REMOVED***

      try ***REMOVED***
        cursor.query = cursor.s.server.s.wireProtocolHandler.command(
          cursor.bson,
          cursor.ns,
          cursor.cmd,
          cursor.s,
          cursor.topology,
          cursor.options
        );

        nextFunction(cursor, callback);
      ***REMOVED*** catch (err) ***REMOVED***
        callback(err);
        return;
      ***REMOVED***
    ***REMOVED***
  );
***REMOVED***

function nextFunction(cursor, callback) ***REMOVED***
  // We have notified about it
  if (cursor.s.notified) ***REMOVED***
    return callback(new Error('cursor is exhausted'));
  ***REMOVED***

  // Cursor is killed return null
  if (isCursorKilled(cursor, callback)) return;

  // Cursor is dead but not marked killed, return null
  if (isCursorDeadButNotkilled(cursor, callback)) return;

  // We have a dead and killed cursor, attempting to call next should error
  if (isCursorDeadAndKilled(cursor, callback)) return;

  // We have just started the cursor
  if (!cursor.s.init) ***REMOVED***
    return initializeCursorAndRetryNext(cursor, callback);
  ***REMOVED***

  // If we don't have a cursorId execute the first query
  if (cursor.s.cursorId == null) ***REMOVED***
    // Check if pool is dead and return if not possible to
    // execute the query against the db
    if (isConnectionDead(cursor, callback)) return;

    // query, cmd, options, s, callback
    return _find(cursor, function(err) ***REMOVED***
      if (err) return handleCallback(callback, err, null);

      if (cursor.s.cursorId && cursor.s.cursorId.isZero() && cursor._endSession) ***REMOVED***
        cursor._endSession();
      ***REMOVED***

      if (
        cursor.s.documents.length === 0 &&
        cursor.s.cursorId &&
        cursor.s.cursorId.isZero() &&
        !cursor.cmd.tailable &&
        !cursor.cmd.awaitData
      ) ***REMOVED***
        return setCursorNotified(cursor, callback);
      ***REMOVED***

      nextFunction(cursor, callback);
    ***REMOVED***);
  ***REMOVED***

  if (cursor.s.documents.length === cursor.s.cursorIndex && Long.ZERO.equals(cursor.s.cursorId)) ***REMOVED***
    setCursorDeadAndNotified(cursor, callback);
    return;
  ***REMOVED***

  if (cursor.s.limit > 0 && cursor.s.currentLimit >= cursor.s.limit) ***REMOVED***
    // Ensure we kill the cursor on the server
    cursor.kill();
    // Set cursor in dead and notified state
    setCursorDeadAndNotified(cursor, callback);
    return;
  ***REMOVED***

  if (
    cursor.s.documents.length === cursor.s.cursorIndex &&
    cursor.cmd.tailable &&
    Long.ZERO.equals(cursor.s.cursorId)
  ) ***REMOVED***
    return handleCallback(
      callback,
      new MongoError(***REMOVED***
        message: 'No more documents in tailed cursor',
        tailable: cursor.cmd.tailable,
        awaitData: cursor.cmd.awaitData
      ***REMOVED***)
    );
  ***REMOVED***

  if (cursor.s.cursorIndex === cursor.s.documents.length && !Long.ZERO.equals(cursor.s.cursorId)) ***REMOVED***
    // Ensure an empty cursor state
    cursor.s.documents = [];
    cursor.s.cursorIndex = 0;

    // Check if connection is dead and return if not possible to
    if (isConnectionDead(cursor, callback)) return;

    // Execute the next get more
    return _getmore(cursor, function(err, doc, connection) ***REMOVED***
      if (err) ***REMOVED***
        if (err instanceof MongoError) ***REMOVED***
          err[mongoErrorContextSymbol].isGetMore = true;
        ***REMOVED***

        return handleCallback(callback, err);
      ***REMOVED***

      if (cursor.s.cursorId && cursor.s.cursorId.isZero() && cursor._endSession) ***REMOVED***
        cursor._endSession();
      ***REMOVED***

      // Save the returned connection to ensure all getMore's fire over the same connection
      cursor.connection = connection;

      // Tailable cursor getMore result, notify owner about it
      // No attempt is made here to retry, this is left to the user of the
      // core module to handle to keep core simple
      if (
        cursor.s.documents.length === 0 &&
        cursor.cmd.tailable &&
        Long.ZERO.equals(cursor.s.cursorId)
      ) ***REMOVED***
        // No more documents in the tailed cursor
        return handleCallback(
          callback,
          new MongoError(***REMOVED***
            message: 'No more documents in tailed cursor',
            tailable: cursor.cmd.tailable,
            awaitData: cursor.cmd.awaitData
          ***REMOVED***)
        );
      ***REMOVED*** else if (
        cursor.s.documents.length === 0 &&
        cursor.cmd.tailable &&
        !Long.ZERO.equals(cursor.s.cursorId)
      ) ***REMOVED***
        return nextFunction(cursor, callback);
      ***REMOVED***

      if (cursor.s.limit > 0 && cursor.s.currentLimit >= cursor.s.limit) ***REMOVED***
        return setCursorDeadAndNotified(cursor, callback);
      ***REMOVED***

      nextFunction(cursor, callback);
    ***REMOVED***);
  ***REMOVED***

  if (cursor.s.limit > 0 && cursor.s.currentLimit >= cursor.s.limit) ***REMOVED***
    // Ensure we kill the cursor on the server
    cursor.kill();
    // Set cursor in dead and notified state
    return setCursorDeadAndNotified(cursor, callback);
  ***REMOVED***

  // Increment the current cursor limit
  cursor.s.currentLimit += 1;

  // Get the document
  let doc = cursor.s.documents[cursor.s.cursorIndex++];

  // Doc overflow
  if (!doc || doc.$err) ***REMOVED***
    // Ensure we kill the cursor on the server
    cursor.kill();
    // Set cursor in dead and notified state
    return setCursorDeadAndNotified(cursor, function() ***REMOVED***
      handleCallback(callback, new MongoError(doc ? doc.$err : undefined));
    ***REMOVED***);
  ***REMOVED***

  // Transform the doc with passed in transformation method if provided
  if (cursor.s.transforms && typeof cursor.s.transforms.doc === 'function') ***REMOVED***
    doc = cursor.s.transforms.doc(doc);
  ***REMOVED***

  // Return the document
  handleCallback(callback, null, doc);
***REMOVED***

module.exports = Cursor;
