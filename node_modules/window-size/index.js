/*
 * window-size
 * https://github.com/jonschlinkert/window-size
 *
 * Copyright (c) 2014 Jon Schlinkert
 * Licensed under the MIT license.
 */

const tty = require('tty')

module.exports = (function() ***REMOVED***
  var width;
  var height;

  if(tty.isatty(1) && tty.isatty(2)) ***REMOVED***
    if(process.stdout.getWindowSize) ***REMOVED***
      width = process.stdout.getWindowSize(1)[0];
      height = process.stdout.getWindowSize(1)[1];
    ***REMOVED*** else if (tty.getWindowSize) ***REMOVED***
      width = tty.getWindowSize()[1];
      height = tty.getWindowSize()[0];
    ***REMOVED*** else if (process.stdout.columns && process.stdout.rows) ***REMOVED***
      height = process.stdout.columns;
      width = process.stdout.rows;
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    new Error('Error: could not get window size with tty or process.stdout');
  ***REMOVED***
  return ***REMOVED***
    height: height,
    width: width
  ***REMOVED***
***REMOVED***)();