/*!
 * content-disposition
 * Copyright(c) 2014 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict'

/**
 * Module exports.
 */

module.exports = contentDisposition
module.exports.parse = parse

/**
 * Module dependencies.
 */

var basename = require('path').basename

/**
 * RegExp to match non attr-char, *after* encodeURIComponent (i.e. not including "%")
 */

var ENCODE_URL_ATTR_CHAR_REGEXP = /[\x00-\x20"'()*,/:;<=>?@[\\\]***REMOVED******REMOVED***\x7f]/g // eslint-disable-line no-control-regex

/**
 * RegExp to match percent encoding escape.
 */

var HEX_ESCAPE_REGEXP = /%[0-9A-Fa-f]***REMOVED***2***REMOVED***/
var HEX_ESCAPE_REPLACE_REGEXP = /%([0-9A-Fa-f]***REMOVED***2***REMOVED***)/g

/**
 * RegExp to match non-latin1 characters.
 */

var NON_LATIN1_REGEXP = /[^\x20-\x7e\xa0-\xff]/g

/**
 * RegExp to match quoted-pair in RFC 2616
 *
 * quoted-pair = "\" CHAR
 * CHAR        = <any US-ASCII character (octets 0 - 127)>
 */

var QESC_REGEXP = /\\([\u0000-\u007f])/g

/**
 * RegExp to match chars that must be quoted-pair in RFC 2616
 */

var QUOTE_REGEXP = /([\\"])/g

/**
 * RegExp for various RFC 2616 grammar
 *
 * parameter     = token "=" ( token | quoted-string )
 * token         = 1*<any CHAR except CTLs or separators>
 * separators    = "(" | ")" | "<" | ">" | "@"
 *               | "," | ";" | ":" | "\" | <">
 *               | "/" | "[" | "]" | "?" | "="
 *               | "***REMOVED***" | "***REMOVED***" | SP | HT
 * quoted-string = ( <"> *(qdtext | quoted-pair ) <"> )
 * qdtext        = <any TEXT except <">>
 * quoted-pair   = "\" CHAR
 * CHAR          = <any US-ASCII character (octets 0 - 127)>
 * TEXT          = <any OCTET except CTLs, but including LWS>
 * LWS           = [CRLF] 1*( SP | HT )
 * CRLF          = CR LF
 * CR            = <US-ASCII CR, carriage return (13)>
 * LF            = <US-ASCII LF, linefeed (10)>
 * SP            = <US-ASCII SP, space (32)>
 * HT            = <US-ASCII HT, horizontal-tab (9)>
 * CTL           = <any US-ASCII control character (octets 0 - 31) and DEL (127)>
 * OCTET         = <any 8-bit sequence of data>
 */

var PARAM_REGEXP = /;[\x09\x20]*([!#$%&'*+.0-9A-Z^_`a-z|~-]+)[\x09\x20]*=[\x09\x20]*("(?:[\x20!\x23-\x5b\x5d-\x7e\x80-\xff]|\\[\x20-\x7e])*"|[!#$%&'*+.0-9A-Z^_`a-z|~-]+)[\x09\x20]*/g // eslint-disable-line no-control-regex
var TEXT_REGEXP = /^[\x20-\x7e\x80-\xff]+$/
var TOKEN_REGEXP = /^[!#$%&'*+.0-9A-Z^_`a-z|~-]+$/

/**
 * RegExp for various RFC 5987 grammar
 *
 * ext-value     = charset  "'" [ language ] "'" value-chars
 * charset       = "UTF-8" / "ISO-8859-1" / mime-charset
 * mime-charset  = 1*mime-charsetc
 * mime-charsetc = ALPHA / DIGIT
 *               / "!" / "#" / "$" / "%" / "&"
 *               / "+" / "-" / "^" / "_" / "`"
 *               / "***REMOVED***" / "***REMOVED***" / "~"
 * language      = ( 2*3ALPHA [ extlang ] )
 *               / 4ALPHA
 *               / 5*8ALPHA
 * extlang       = *3( "-" 3ALPHA )
 * value-chars   = *( pct-encoded / attr-char )
 * pct-encoded   = "%" HEXDIG HEXDIG
 * attr-char     = ALPHA / DIGIT
 *               / "!" / "#" / "$" / "&" / "+" / "-" / "."
 *               / "^" / "_" / "`" / "|" / "~"
 */

var EXT_VALUE_REGEXP = /^([A-Za-z0-9!#$%&+\-^_`***REMOVED******REMOVED***~]+)'(?:[A-Za-z]***REMOVED***2,3***REMOVED***(?:-[A-Za-z]***REMOVED***3***REMOVED***)***REMOVED***0,3***REMOVED***|[A-Za-z]***REMOVED***4,8***REMOVED***|)'((?:%[0-9A-Fa-f]***REMOVED***2***REMOVED***|[A-Za-z0-9!#$&+.^_`|~-])+)$/

/**
 * RegExp for various RFC 6266 grammar
 *
 * disposition-type = "inline" | "attachment" | disp-ext-type
 * disp-ext-type    = token
 * disposition-parm = filename-parm | disp-ext-parm
 * filename-parm    = "filename" "=" value
 *                  | "filename*" "=" ext-value
 * disp-ext-parm    = token "=" value
 *                  | ext-token "=" ext-value
 * ext-token        = <the characters in token, followed by "*">
 */

var DISPOSITION_TYPE_REGEXP = /^([!#$%&'*+.0-9A-Z^_`a-z|~-]+)[\x09\x20]*(?:$|;)/ // eslint-disable-line no-control-regex

/**
 * Create an attachment Content-Disposition header.
 *
 * @param ***REMOVED***string***REMOVED*** [filename]
 * @param ***REMOVED***object***REMOVED*** [options]
 * @param ***REMOVED***string***REMOVED*** [options.type=attachment]
 * @param ***REMOVED***string|boolean***REMOVED*** [options.fallback=true]
 * @return ***REMOVED***string***REMOVED***
 * @api public
 */

function contentDisposition (filename, options) ***REMOVED***
  var opts = options || ***REMOVED******REMOVED***

  // get type
  var type = opts.type || 'attachment'

  // get parameters
  var params = createparams(filename, opts.fallback)

  // format into string
  return format(new ContentDisposition(type, params))
***REMOVED***

/**
 * Create parameters object from filename and fallback.
 *
 * @param ***REMOVED***string***REMOVED*** [filename]
 * @param ***REMOVED***string|boolean***REMOVED*** [fallback=true]
 * @return ***REMOVED***object***REMOVED***
 * @api private
 */

function createparams (filename, fallback) ***REMOVED***
  if (filename === undefined) ***REMOVED***
    return
  ***REMOVED***

  var params = ***REMOVED******REMOVED***

  if (typeof filename !== 'string') ***REMOVED***
    throw new TypeError('filename must be a string')
  ***REMOVED***

  // fallback defaults to true
  if (fallback === undefined) ***REMOVED***
    fallback = true
  ***REMOVED***

  if (typeof fallback !== 'string' && typeof fallback !== 'boolean') ***REMOVED***
    throw new TypeError('fallback must be a string or boolean')
  ***REMOVED***

  if (typeof fallback === 'string' && NON_LATIN1_REGEXP.test(fallback)) ***REMOVED***
    throw new TypeError('fallback must be ISO-8859-1 string')
  ***REMOVED***

  // restrict to file base name
  var name = basename(filename)

  // determine if name is suitable for quoted string
  var isQuotedString = TEXT_REGEXP.test(name)

  // generate fallback name
  var fallbackName = typeof fallback !== 'string'
    ? fallback && getlatin1(name)
    : basename(fallback)
  var hasFallback = typeof fallbackName === 'string' && fallbackName !== name

  // set extended filename parameter
  if (hasFallback || !isQuotedString || HEX_ESCAPE_REGEXP.test(name)) ***REMOVED***
    params['filename*'] = name
  ***REMOVED***

  // set filename parameter
  if (isQuotedString || hasFallback) ***REMOVED***
    params.filename = hasFallback
      ? fallbackName
      : name
  ***REMOVED***

  return params
***REMOVED***

/**
 * Format object to Content-Disposition header.
 *
 * @param ***REMOVED***object***REMOVED*** obj
 * @param ***REMOVED***string***REMOVED*** obj.type
 * @param ***REMOVED***object***REMOVED*** [obj.parameters]
 * @return ***REMOVED***string***REMOVED***
 * @api private
 */

function format (obj) ***REMOVED***
  var parameters = obj.parameters
  var type = obj.type

  if (!type || typeof type !== 'string' || !TOKEN_REGEXP.test(type)) ***REMOVED***
    throw new TypeError('invalid type')
  ***REMOVED***

  // start with normalized type
  var string = String(type).toLowerCase()

  // append parameters
  if (parameters && typeof parameters === 'object') ***REMOVED***
    var param
    var params = Object.keys(parameters).sort()

    for (var i = 0; i < params.length; i++) ***REMOVED***
      param = params[i]

      var val = param.substr(-1) === '*'
        ? ustring(parameters[param])
        : qstring(parameters[param])

      string += '; ' + param + '=' + val
    ***REMOVED***
  ***REMOVED***

  return string
***REMOVED***

/**
 * Decode a RFC 6987 field value (gracefully).
 *
 * @param ***REMOVED***string***REMOVED*** str
 * @return ***REMOVED***string***REMOVED***
 * @api private
 */

function decodefield (str) ***REMOVED***
  var match = EXT_VALUE_REGEXP.exec(str)

  if (!match) ***REMOVED***
    throw new TypeError('invalid extended field value')
  ***REMOVED***

  var charset = match[1].toLowerCase()
  var encoded = match[2]
  var value

  // to binary string
  var binary = encoded.replace(HEX_ESCAPE_REPLACE_REGEXP, pdecode)

  switch (charset) ***REMOVED***
    case 'iso-8859-1':
      value = getlatin1(binary)
      break
    case 'utf-8':
      value = new Buffer(binary, 'binary').toString('utf8')
      break
    default:
      throw new TypeError('unsupported charset in extended field')
  ***REMOVED***

  return value
***REMOVED***

/**
 * Get ISO-8859-1 version of string.
 *
 * @param ***REMOVED***string***REMOVED*** val
 * @return ***REMOVED***string***REMOVED***
 * @api private
 */

function getlatin1 (val) ***REMOVED***
  // simple Unicode -> ISO-8859-1 transformation
  return String(val).replace(NON_LATIN1_REGEXP, '?')
***REMOVED***

/**
 * Parse Content-Disposition header string.
 *
 * @param ***REMOVED***string***REMOVED*** string
 * @return ***REMOVED***object***REMOVED***
 * @api private
 */

function parse (string) ***REMOVED***
  if (!string || typeof string !== 'string') ***REMOVED***
    throw new TypeError('argument string is required')
  ***REMOVED***

  var match = DISPOSITION_TYPE_REGEXP.exec(string)

  if (!match) ***REMOVED***
    throw new TypeError('invalid type format')
  ***REMOVED***

  // normalize type
  var index = match[0].length
  var type = match[1].toLowerCase()

  var key
  var names = []
  var params = ***REMOVED******REMOVED***
  var value

  // calculate index to start at
  index = PARAM_REGEXP.lastIndex = match[0].substr(-1) === ';'
    ? index - 1
    : index

  // match parameters
  while ((match = PARAM_REGEXP.exec(string))) ***REMOVED***
    if (match.index !== index) ***REMOVED***
      throw new TypeError('invalid parameter format')
    ***REMOVED***

    index += match[0].length
    key = match[1].toLowerCase()
    value = match[2]

    if (names.indexOf(key) !== -1) ***REMOVED***
      throw new TypeError('invalid duplicate parameter')
    ***REMOVED***

    names.push(key)

    if (key.indexOf('*') + 1 === key.length) ***REMOVED***
      // decode extended value
      key = key.slice(0, -1)
      value = decodefield(value)

      // overwrite existing value
      params[key] = value
      continue
    ***REMOVED***

    if (typeof params[key] === 'string') ***REMOVED***
      continue
    ***REMOVED***

    if (value[0] === '"') ***REMOVED***
      // remove quotes and escapes
      value = value
        .substr(1, value.length - 2)
        .replace(QESC_REGEXP, '$1')
    ***REMOVED***

    params[key] = value
  ***REMOVED***

  if (index !== -1 && index !== string.length) ***REMOVED***
    throw new TypeError('invalid parameter format')
  ***REMOVED***

  return new ContentDisposition(type, params)
***REMOVED***

/**
 * Percent decode a single character.
 *
 * @param ***REMOVED***string***REMOVED*** str
 * @param ***REMOVED***string***REMOVED*** hex
 * @return ***REMOVED***string***REMOVED***
 * @api private
 */

function pdecode (str, hex) ***REMOVED***
  return String.fromCharCode(parseInt(hex, 16))
***REMOVED***

/**
 * Percent encode a single character.
 *
 * @param ***REMOVED***string***REMOVED*** char
 * @return ***REMOVED***string***REMOVED***
 * @api private
 */

function pencode (char) ***REMOVED***
  var hex = String(char)
    .charCodeAt(0)
    .toString(16)
    .toUpperCase()
  return hex.length === 1
    ? '%0' + hex
    : '%' + hex
***REMOVED***

/**
 * Quote a string for HTTP.
 *
 * @param ***REMOVED***string***REMOVED*** val
 * @return ***REMOVED***string***REMOVED***
 * @api private
 */

function qstring (val) ***REMOVED***
  var str = String(val)

  return '"' + str.replace(QUOTE_REGEXP, '\\$1') + '"'
***REMOVED***

/**
 * Encode a Unicode string for HTTP (RFC 5987).
 *
 * @param ***REMOVED***string***REMOVED*** val
 * @return ***REMOVED***string***REMOVED***
 * @api private
 */

function ustring (val) ***REMOVED***
  var str = String(val)

  // percent encode as UTF-8
  var encoded = encodeURIComponent(str)
    .replace(ENCODE_URL_ATTR_CHAR_REGEXP, pencode)

  return 'UTF-8\'\'' + encoded
***REMOVED***

/**
 * Class for parsed Content-Disposition header for v8 optimization
 */

function ContentDisposition (type, parameters) ***REMOVED***
  this.type = type
  this.parameters = parameters
***REMOVED***
