var baseRest = require('./_baseRest'),
    eq = require('./eq'),
    isIterateeCall = require('./_isIterateeCall'),
    keysIn = require('./keysIn');

/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Assigns own and inherited enumerable string keyed properties of source
 * objects to the destination object for all destination properties that
 * resolve to `undefined`. Source objects are applied from left to right.
 * Once a property is set, additional values of the same property are ignored.
 *
 * **Note:** This method mutates `object`.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param ***REMOVED***Object***REMOVED*** object The destination object.
 * @param ***REMOVED***...Object***REMOVED*** [sources] The source objects.
 * @returns ***REMOVED***Object***REMOVED*** Returns `object`.
 * @see _.defaultsDeep
 * @example
 *
 * _.defaults(***REMOVED*** 'a': 1 ***REMOVED***, ***REMOVED*** 'b': 2 ***REMOVED***, ***REMOVED*** 'a': 3 ***REMOVED***);
 * // => ***REMOVED*** 'a': 1, 'b': 2 ***REMOVED***
 */
var defaults = baseRest(function(object, sources) ***REMOVED***
  object = Object(object);

  var index = -1;
  var length = sources.length;
  var guard = length > 2 ? sources[2] : undefined;

  if (guard && isIterateeCall(sources[0], sources[1], guard)) ***REMOVED***
    length = 1;
  ***REMOVED***

  while (++index < length) ***REMOVED***
    var source = sources[index];
    var props = keysIn(source);
    var propsIndex = -1;
    var propsLength = props.length;

    while (++propsIndex < propsLength) ***REMOVED***
      var key = props[propsIndex];
      var value = object[key];

      if (value === undefined ||
          (eq(value, objectProto[key]) && !hasOwnProperty.call(object, key))) ***REMOVED***
        object[key] = source[key];
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  return object;
***REMOVED***);

module.exports = defaults;
