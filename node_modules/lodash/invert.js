var constant = require('./constant'),
    createInverter = require('./_createInverter'),
    identity = require('./identity');

/** Used for built-in method references. */
var objectProto = Object.prototype;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/**
 * Creates an object composed of the inverted keys and values of `object`.
 * If `object` contains duplicate values, subsequent values overwrite
 * property assignments of previous values.
 *
 * @static
 * @memberOf _
 * @since 0.7.0
 * @category Object
 * @param ***REMOVED***Object***REMOVED*** object The object to invert.
 * @returns ***REMOVED***Object***REMOVED*** Returns the new inverted object.
 * @example
 *
 * var object = ***REMOVED*** 'a': 1, 'b': 2, 'c': 1 ***REMOVED***;
 *
 * _.invert(object);
 * // => ***REMOVED*** '1': 'c', '2': 'b' ***REMOVED***
 */
var invert = createInverter(function(result, value, key) ***REMOVED***
  if (value != null &&
      typeof value.toString != 'function') ***REMOVED***
    value = nativeObjectToString.call(value);
  ***REMOVED***

  result[value] = key;
***REMOVED***, constant(identity));

module.exports = invert;
