/**
 * JSONSchema Validator - Validates JavaScript objects using JSON Schemas
 *	(http://www.json.com/json-schema-proposal/)
 *
 * Copyright (c) 2007 Kris Zyp SitePen (www.sitepen.com)
 * Licensed under the MIT (MIT-LICENSE.txt) license.
To use the validator call the validate function with an instance object and an optional schema object.
If a schema is provided, it will be used to validate. If the instance object refers to a schema (self-validating),
that schema will be used to validate and the schema parameter is not necessary (if both exist,
both validations will occur).
The validate method will return an array of validation errors. If there are no errors, then an
empty list will be returned. A validation error will have two properties:
"property" which indicates which property had the error
"message" which indicates what the error was
 */
(function (root, factory) ***REMOVED***
    if (typeof define === 'function' && define.amd) ***REMOVED***
        // AMD. Register as an anonymous module.
        define([], function () ***REMOVED***
            return factory();
        ***REMOVED***);
    ***REMOVED*** else if (typeof module === 'object' && module.exports) ***REMOVED***
        // Node. Does not work with strict CommonJS, but
        // only CommonJS-like environments that support module.exports,
        // like Node.
        module.exports = factory();
    ***REMOVED*** else ***REMOVED***
        // Browser globals
        root.jsonSchema = factory();
    ***REMOVED***
***REMOVED***(this, function () ***REMOVED***// setup primitive classes to be JSON Schema types
var exports = validate
exports.Integer = ***REMOVED***type:"integer"***REMOVED***;
var primitiveConstructors = ***REMOVED***
	String: String,
	Boolean: Boolean,
	Number: Number,
	Object: Object,
	Array: Array,
	Date: Date
***REMOVED***
exports.validate = validate;
function validate(/*Any*/instance,/*Object*/schema) ***REMOVED***
		// Summary:
		//  	To use the validator call JSONSchema.validate with an instance object and an optional schema object.
		// 		If a schema is provided, it will be used to validate. If the instance object refers to a schema (self-validating),
		// 		that schema will be used to validate and the schema parameter is not necessary (if both exist,
		// 		both validations will occur).
		// 		The validate method will return an object with two properties:
		// 			valid: A boolean indicating if the instance is valid by the schema
		// 			errors: An array of validation errors. If there are no errors, then an
		// 					empty list will be returned. A validation error will have two properties:
		// 						property: which indicates which property had the error
		// 						message: which indicates what the error was
		//
		return validate(instance, schema, ***REMOVED***changing: false***REMOVED***);//, coerce: false, existingOnly: false***REMOVED***);
	***REMOVED***;
exports.checkPropertyChange = function(/*Any*/value,/*Object*/schema, /*String*/property) ***REMOVED***
		// Summary:
		// 		The checkPropertyChange method will check to see if an value can legally be in property with the given schema
		// 		This is slightly different than the validate method in that it will fail if the schema is readonly and it will
		// 		not check for self-validation, it is assumed that the passed in value is already internally valid.
		// 		The checkPropertyChange method will return the same object type as validate, see JSONSchema.validate for
		// 		information.
		//
		return validate(value, schema, ***REMOVED***changing: property || "property"***REMOVED***);
	***REMOVED***;
var validate = exports._validate = function(/*Any*/instance,/*Object*/schema,/*Object*/options) ***REMOVED***

	if (!options) options = ***REMOVED******REMOVED***;
	var _changing = options.changing;

	function getType(schema)***REMOVED***
		return schema.type || (primitiveConstructors[schema.name] == schema && schema.name.toLowerCase());
	***REMOVED***
	var errors = [];
	// validate a value against a property definition
	function checkProp(value, schema, path,i)***REMOVED***

		var l;
		path += path ? typeof i == 'number' ? '[' + i + ']' : typeof i == 'undefined' ? '' : '.' + i : i;
		function addError(message)***REMOVED***
			errors.push(***REMOVED***property:path,message:message***REMOVED***);
		***REMOVED***

		if((typeof schema != 'object' || schema instanceof Array) && (path || typeof schema != 'function') && !(schema && getType(schema)))***REMOVED***
			if(typeof schema == 'function')***REMOVED***
				if(!(value instanceof schema))***REMOVED***
					addError("is not an instance of the class/constructor " + schema.name);
				***REMOVED***
			***REMOVED***else if(schema)***REMOVED***
				addError("Invalid schema/property definition " + schema);
			***REMOVED***
			return null;
		***REMOVED***
		if(_changing && schema.readonly)***REMOVED***
			addError("is a readonly field, it can not be changed");
		***REMOVED***
		if(schema['extends'])***REMOVED*** // if it extends another schema, it must pass that schema as well
			checkProp(value,schema['extends'],path,i);
		***REMOVED***
		// validate a value against a type definition
		function checkType(type,value)***REMOVED***
			if(type)***REMOVED***
				if(typeof type == 'string' && type != 'any' &&
						(type == 'null' ? value !== null : typeof value != type) &&
						!(value instanceof Array && type == 'array') &&
						!(value instanceof Date && type == 'date') &&
						!(type == 'integer' && value%1===0))***REMOVED***
					return [***REMOVED***property:path,message:(typeof value) + " value found, but a " + type + " is required"***REMOVED***];
				***REMOVED***
				if(type instanceof Array)***REMOVED***
					var unionErrors=[];
					for(var j = 0; j < type.length; j++)***REMOVED*** // a union type
						if(!(unionErrors=checkType(type[j],value)).length)***REMOVED***
							break;
						***REMOVED***
					***REMOVED***
					if(unionErrors.length)***REMOVED***
						return unionErrors;
					***REMOVED***
				***REMOVED***else if(typeof type == 'object')***REMOVED***
					var priorErrors = errors;
					errors = [];
					checkProp(value,type,path);
					var theseErrors = errors;
					errors = priorErrors;
					return theseErrors;
				***REMOVED***
			***REMOVED***
			return [];
		***REMOVED***
		if(value === undefined)***REMOVED***
			if(schema.required)***REMOVED***
				addError("is missing and it is required");
			***REMOVED***
		***REMOVED***else***REMOVED***
			errors = errors.concat(checkType(getType(schema),value));
			if(schema.disallow && !checkType(schema.disallow,value).length)***REMOVED***
				addError(" disallowed value was matched");
			***REMOVED***
			if(value !== null)***REMOVED***
				if(value instanceof Array)***REMOVED***
					if(schema.items)***REMOVED***
						var itemsIsArray = schema.items instanceof Array;
						var propDef = schema.items;
						for (i = 0, l = value.length; i < l; i += 1) ***REMOVED***
							if (itemsIsArray)
								propDef = schema.items[i];
							if (options.coerce)
								value[i] = options.coerce(value[i], propDef);
							errors.concat(checkProp(value[i],propDef,path,i));
						***REMOVED***
					***REMOVED***
					if(schema.minItems && value.length < schema.minItems)***REMOVED***
						addError("There must be a minimum of " + schema.minItems + " in the array");
					***REMOVED***
					if(schema.maxItems && value.length > schema.maxItems)***REMOVED***
						addError("There must be a maximum of " + schema.maxItems + " in the array");
					***REMOVED***
				***REMOVED***else if(schema.properties || schema.additionalProperties)***REMOVED***
					errors.concat(checkObj(value, schema.properties, path, schema.additionalProperties));
				***REMOVED***
				if(schema.pattern && typeof value == 'string' && !value.match(schema.pattern))***REMOVED***
					addError("does not match the regex pattern " + schema.pattern);
				***REMOVED***
				if(schema.maxLength && typeof value == 'string' && value.length > schema.maxLength)***REMOVED***
					addError("may only be " + schema.maxLength + " characters long");
				***REMOVED***
				if(schema.minLength && typeof value == 'string' && value.length < schema.minLength)***REMOVED***
					addError("must be at least " + schema.minLength + " characters long");
				***REMOVED***
				if(typeof schema.minimum !== undefined && typeof value == typeof schema.minimum &&
						schema.minimum > value)***REMOVED***
					addError("must have a minimum value of " + schema.minimum);
				***REMOVED***
				if(typeof schema.maximum !== undefined && typeof value == typeof schema.maximum &&
						schema.maximum < value)***REMOVED***
					addError("must have a maximum value of " + schema.maximum);
				***REMOVED***
				if(schema['enum'])***REMOVED***
					var enumer = schema['enum'];
					l = enumer.length;
					var found;
					for(var j = 0; j < l; j++)***REMOVED***
						if(enumer[j]===value)***REMOVED***
							found=1;
							break;
						***REMOVED***
					***REMOVED***
					if(!found)***REMOVED***
						addError("does not have a value in the enumeration " + enumer.join(", "));
					***REMOVED***
				***REMOVED***
				if(typeof schema.maxDecimal == 'number' &&
					(value.toString().match(new RegExp("\\.[0-9]***REMOVED***" + (schema.maxDecimal + 1) + ",***REMOVED***"))))***REMOVED***
					addError("may only have " + schema.maxDecimal + " digits of decimal places");
				***REMOVED***
			***REMOVED***
		***REMOVED***
		return null;
	***REMOVED***
	// validate an object against a schema
	function checkObj(instance,objTypeDef,path,additionalProp)***REMOVED***

		if(typeof objTypeDef =='object')***REMOVED***
			if(typeof instance != 'object' || instance instanceof Array)***REMOVED***
				errors.push(***REMOVED***property:path,message:"an object is required"***REMOVED***);
			***REMOVED***
			
			for(var i in objTypeDef)***REMOVED*** 
				if(objTypeDef.hasOwnProperty(i))***REMOVED***
					var value = instance[i];
					// skip _not_ specified properties
					if (value === undefined && options.existingOnly) continue;
					var propDef = objTypeDef[i];
					// set default
					if(value === undefined && propDef["default"])***REMOVED***
						value = instance[i] = propDef["default"];
					***REMOVED***
					if(options.coerce && i in instance)***REMOVED***
						value = instance[i] = options.coerce(value, propDef);
					***REMOVED***
					checkProp(value,propDef,path,i);
				***REMOVED***
			***REMOVED***
		***REMOVED***
		for(i in instance)***REMOVED***
			if(instance.hasOwnProperty(i) && !(i.charAt(0) == '_' && i.charAt(1) == '_') && objTypeDef && !objTypeDef[i] && additionalProp===false)***REMOVED***
				if (options.filter) ***REMOVED***
					delete instance[i];
					continue;
				***REMOVED*** else ***REMOVED***
					errors.push(***REMOVED***property:path,message:(typeof value) + "The property " + i +
						" is not defined in the schema and the schema does not allow additional properties"***REMOVED***);
				***REMOVED***
			***REMOVED***
			var requires = objTypeDef && objTypeDef[i] && objTypeDef[i].requires;
			if(requires && !(requires in instance))***REMOVED***
				errors.push(***REMOVED***property:path,message:"the presence of the property " + i + " requires that " + requires + " also be present"***REMOVED***);
			***REMOVED***
			value = instance[i];
			if(additionalProp && (!(objTypeDef && typeof objTypeDef == 'object') || !(i in objTypeDef)))***REMOVED***
				if(options.coerce)***REMOVED***
					value = instance[i] = options.coerce(value, additionalProp);
				***REMOVED***
				checkProp(value,additionalProp,path,i);
			***REMOVED***
			if(!_changing && value && value.$schema)***REMOVED***
				errors = errors.concat(checkProp(value,value.$schema,path,i));
			***REMOVED***
		***REMOVED***
		return errors;
	***REMOVED***
	if(schema)***REMOVED***
		checkProp(instance,schema,'',_changing || '');
	***REMOVED***
	if(!_changing && instance && instance.$schema)***REMOVED***
		checkProp(instance,instance.$schema,'','');
	***REMOVED***
	return ***REMOVED***valid:!errors.length,errors:errors***REMOVED***;
***REMOVED***;
exports.mustBeValid = function(result)***REMOVED***
	//	summary:
	//		This checks to ensure that the result is valid and will throw an appropriate error message if it is not
	// result: the result returned from checkPropertyChange or validate
	if(!result.valid)***REMOVED***
		throw new TypeError(result.errors.map(function(error)***REMOVED***return "for property " + error.property + ': ' + error.message;***REMOVED***).join(", \n"));
	***REMOVED***
***REMOVED***

return exports;
***REMOVED***));
